# Session Handoff: academy-platform

<!-- session-handoff/v1
{
  "schema": "session-handoff/v1",
  "id": "20260801T184306949Z-academy-platform",
  "created_at": "2026-08-01T18:43:06.949Z",
  "project": "academy-platform",
  "objective": "ลงมือ implement ตาม plans/implementation-plan-2026-08-02.md ที่ผ่าน RIL 7 รอบ เริ่มที่ W0-0 (โครง attempt + คลังข้อ) ซึ่งเป็นงานแรกที่ทุกอย่างพึ่ง",
  "state": "ready",
  "repo": {
    "remote": "github.com/teerakanok/academy-platform",
    "branch": "main",
    "base_head": "ba8814fee0a50b2025a1a9d3ad48089f6bcf2b50"
  },
  "delivery": "local",
    "worktree": {
    "mode": "allowlisted",
    "entries": [
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/course-roadmap-dark-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/course-roadmap-dark-mobile-390.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/course-roadmap-in-progress-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/dashboard-in-progress-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/handouts-and-links-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/lab-inline-dialog-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/lesson-with-video-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/lesson-with-video-mobile-390.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/video-popquiz-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/course/video-popquiz-mobile-390.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/m1/privacy-th-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/m2/pbq-all-kinds-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/m2/pbq-exhibit-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/m2/quiz-mid-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/m2/quiz-mid-mobile-390.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/m2/results-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      },
      {
        "status": " M",
        "path": "artifacts/oneshot-2026-07-31/m2/review-desktop-1440.png",
        "owner": "continuation",
        "reason": "ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้"
      }
    ]
  },
  "scope": {
    "allowed": [
      "แก้โค้ดใน academy-web/ ตาม W0-0 → W0-1..4 → W1 → W2 → W3 → W4 ของ plans/implementation-plan-2026-08-02.md",
      "เขียน migration ใหม่ใน academy-web/supabase/migrations/ (ตาราง attempt + ตัวนับครั้งแบบถาวร)",
      "เขียนเนื้อหาคลังข้อ capstone ภาษาอังกฤษ (W-content)",
      "เขียนและรันเทส unit/integration/e2e",
      "อัปเดต plans/active_plan.md, plans/completed_log.md, PENDING_USER_ACTION.md, SBOM.md",
      "รัน wrangler dev --remote หรือ preview worker เพื่อ verify /media/*"
    ],
    "forbidden": [
      "deploy ขึ้น production หรือ push โดยไม่ได้รับอนุญาตจาก founder ในรอบนั้น",
      "สร้าง R2 bucket หรือแตะ external service ใดๆ เอง (Supabase console, Cloudflare dashboard, DNS)",
      "รื้อ decision D1–D13 ใน §3 ของแผนโดยไม่ถาม founder",
      "แก้ระบบ Identity Control หรือเปิด /sign-in — session identity เป็นเจ้าของ (§6 ของแผน)",
      "ดึงงานภาษาไทย (W-content) เข้ามาเป็นเกณฑ์ปิด W0",
      "แตะไฟล์ใน artifacts/oneshot-2026-07-31/ นอกจากจะตัดสินใจเรื่อง 17 ไฟล์ที่ dirty อยู่"
    ]
  },
  "canonical_read_order": [
    "AGENTS.md",
    "plans/implementation-plan-2026-08-02.md",
    "plans/active_plan.md",
    "PENDING_USER_ACTION.md",
    "academy-web/SBOM.md"
  ],
  "owner_decisions": [
    "D1–D13 ล็อกแล้วใน §3 ของแผน — รวมถึงต้องสมัครบัญชีก่อนใช้ (กัน burn ค่า infra), บัญชีเดียวใช้ได้ทุก product, frontend อยู่บน Cloudflare",
    "W0-3 ล็อก: บทปกติผ่านเมื่อผิดไม่เกิน 1 ข้อ · retry ไม่จำกัด · และยอมรับว่า completed ไม่ใช่หลักฐาน → น้ำหนักใบรับรองอยู่ที่ capstone ทั้งหมด",
    "W0-4 ล็อก: คำถามกลางวิดีโอเป็น formative ไม่ใช่ด่าน",
    "W0-0 ล็อก: ทำคลังข้อเฉพาะ capstone 3 จุด และปิด test-out บน node ที่ยังไม่มีคลังข้อ",
    "W0-1 ล็อก: /player ไม่แก้ในเฟสนี้ (INTERNAL ONLY ตาม CAS-005) แต่ต้องมีเทสยืนยันว่าเข้าไม่ได้จากภายนอก",
    "founder: lab ไม่ใช่จุดขายที่สำคัญที่สุด — การที่ video + quiz + capstone + simulation + lab รวมเป็น experience เดียวต่างหากคือจุดขาย จึงไม่กลัวการก๊อปเนื้อหาแบบอ่านช้าๆ",
    "founder: session identity เป็นตัวตัดสินเรื่อง central login · Academy ปรับตามภายหลัง"
  ],
  "completed": [
    "craft แผน implement ที่สะท้อน decision/ข้อจำกัด/ข้อเท็จจริงทั้งหมดจากบทสนทนา — plans/implementation-plan-2026-08-02.md",
    "RIL 7 รอบ สองเลนอิสระ (codex model_reasoning_effort=high + Claude critic) จน PASS ทั้งสองเลนในรอบที่ 7",
    "พิสูจน์ด้วยการรันจริงว่าคำอ้างเดิม 'แกนหลักฐานต่อครบแล้ว' เป็นเท็จ — พบ F1–F5 ที่ยังอยู่บน HEAD",
    "แก้ blocking 16 ข้อและข้อเท็จจริงในแผนที่ผิด 8 ข้อ โดยยืนยันซ้ำจากไฟล์จริงทุกข้อก่อนแก้",
    "บันทึกบทเรียนสองข้อเป็น memory พร้อม prevention control",
    "ชี้ให้ active_plan.md ไปที่แผนที่ผ่าน RIL แล้ว"
  ],
  "changed_files": [
    {
      "path": "plans/implementation-plan-2026-08-02.md",
      "reason": "แผน implement ที่ผ่าน RIL — เอกสารหลักที่ session ถัดไปเดินตาม (commit ba8814f)"
    },
    {
      "path": "plans/active_plan.md",
      "reason": "เพิ่มหัวข้อชี้ไปที่แผนที่ผ่าน RIL พร้อมตาราง F1–F5 และลำดับงานที่บังคับ (commit ba8814f)"
    },
    {
      "path": "artifacts/oneshot-2026-07-31/",
      "reason": "17 PNG ที่ถูก render ใหม่จากการรัน e2e — ยัง dirty ไม่ได้ commit"
    }
  ],
  "remaining_work": [
    "W0-0: ตาราง attempt + POST /api/attempts + atomic consume ที่มี ownership/context/expiry ใน WHERE เดียว + ตัวนับครั้งแบบถาวร atomic",
    "W0-0: คลังข้อ capstone EN 39 ข้อ (มี 13 ต้องเขียนเพิ่ม 26) + remap key ต่อ attempt + ปิด test-out บน node ที่ไม่มีคลังข้อ",
    "W0-1: แยก server-only answer-key model ออกจาก public DTO ทั้ง 4 พื้นผิว + assessed คืน {passed} เท่านั้น + สัญญา practice",
    "W0-2: UI ต้องรอ outcome จากเซิร์ฟเวอร์ก่อน setDone",
    "W0-3: certificateEligibility แยกสองเงื่อนไข (ทุก node completed/tested-out และทุก capstone ผ่านแบบ assessed)",
    "W0-4: แก้เอกสารทุกที่ที่เรียกคำถามกลางวิดีโอว่าแกนหลักฐาน",
    "W1: ต่อ simulation เข้า checkpoint + เขียน simulation ลง capstone จริงอย่างน้อย 1 จุด",
    "W2: W2-0 เรื่อง /media/* ก่อน แล้วค่อย guard กลาง authorizeCourseResource + entitlement",
    "W3: เพิ่ม capstone จุดที่ 3 ใน basic-os-linux + bump version 1.1.0",
    "W4: ใบรับรองที่ตรวจสอบได้ (หลัง W0+W1+W3)",
    "W5/W6: bot detection · surface จำลองอันที่สอง",
    "W-content: บทเรียนภาษาไทย (ทั้ง repo มีไฟล์เดียว)",
    "หนี้เอกสาร: PENDING_USER_ACTION.md ยังอ้าง Vercel และ migration 0001 อย่างเดียว ทั้งที่มี 0002–0004 และ deploy อยู่บน Cloudflare — แก้ก่อนลงมือ W0"
  ],
  "risks": [
    "W2 มี founder blocker สองตัวที่ยังไม่ได้ขอ: R2 bucket และการอนุญาต deploy หนึ่งครั้งเพื่อ verify /media/* — ต้องเพิ่มลง PENDING_USER_ACTION.md ตั้งแต่เปิด session ไม่ใช่ค้นพบตอนติด",
    "W0-0 มีงานเนื้อหา 26 ข้อที่ไม่ใช่งานโค้ด — ถ้าไม่แยกนับจะกลายเป็นของค้างที่บล็อกทุกอย่างข้างหลัง",
    "โควตา 3 ครั้ง/30 นาที ผูกกับบัญชี ซึ่งเลี่ยงได้ด้วยการสมัครใหม่ (D1 เปิดสมัครเสรี) — เป็นแค่ speed bump จนกว่า W5 จะทำให้การสมัครมีต้นทุน",
    "e2e ที่รันผ่าน npm run start จะเขียวปลอมสำหรับ /media/* เพราะไม่มี ASSETS binding",
    "simulation checkpoint พิสูจน์แค่ว่า 'รู้คำตอบที่ถูก' ไม่ใช่ 'ลงมือทำจริง' — ห้ามเคลมเกินนี้บนหน้าเว็บหรือบนใบรับรอง",
    "session identity อาจเคาะให้มี central sign-in ซึ่งจะทำให้ /sign-in กลายเป็น redirect — อย่าลงแรงกับหน้านั้นเพิ่ม"
  ],
  "next": {
    "cwd": "academy-web",
    "summary": "เริ่ม W0-0: สร้างตาราง attempt + endpoint POST /api/attempts + atomic consume แล้วต่อด้วยคลังข้อ capstone",
    "first_step": "อ่าน plans/implementation-plan-2026-08-02.md §0 (F1–F5) และ §5 W0-0 ให้ครบ แล้วแก้ PENDING_USER_ACTION.md ให้ตรงความจริง (Vercel → Cloudflare, migration 0001 → 0001–0004) พร้อมเพิ่มสองรายการใน §4.2b (R2 bucket + ขออนุญาต deploy verify /media/*) จากนั้นเขียน migration 0005 สร้างตาราง academy.attempt ตาม field ในตารางของ W0-0",
    "commands": [
      "cd academy-web",
      "npm run build && npm run lint && npm run test && npm run test:e2e"
    ],
    "acceptance": [
      "migration 0005 สร้างตาราง academy.attempt ครบทุก field ใน W0-0 (attempt_id, user_id, course_slug, node_id, challenge_id, params, challenge_version, expires_at, consumed_at) และ RLS เปิดแบบ default deny เหมือนตารางอื่นในสคีมา",
      "POST /api/attempts ออก attempt ที่เดาไม่ได้ และผูกกับผู้ใช้ที่ล็อกอินเท่านั้น",
      "consume เป็นคำสั่งเดียวที่มี attempt_id + user_id + course_slug + node_id + challenge_id + consumed_at is null + expires_at > now() อยู่ใน WHERE เดียวกัน",
      "เทส race: ยิง attempt_id เดียวกันพร้อมกันสองเส้น → ผ่านได้อย่างมากหนึ่งเส้น",
      "เทส: ใช้ attempt_id ของผู้ใช้คนอื่น / ของ node อื่น / ที่หมดอายุ → ถูกปฏิเสธ",
      "npm run build && npm run lint && npm run test && npm run test:e2e เขียวทั้งหมด"
    ],
    "execution_boundary": "local-reversible"
  },
  "blocker": null,
  "verification": [
    {
      "command": "codex exec -c model_reasoning_effort=high (RIL รอบที่ 7 บน plans/implementation-plan-2026-08-02.md)",
      "result": "PASS — 'ไม่เหลือ blocker; มีเพียงหัวข้อ W0 ซ้ำหนึ่งครั้ง เป็น editorial เท่านั้น' (แก้แล้ว)"
    },
    {
      "command": "Claude critic subagent (RIL รอบที่ 7 — ตรวจการแก้ 6 จุดกับไฟล์จริงและตรวจทุกบรรทัดโค้ดที่แผนอ้างถึงซ้ำ)",
      "result": "PASS — ยืนยันว่าการแก้ทั้ง 6 จุดอยู่ในไฟล์จริงและถูกต้อง และโค้ดที่อ้างตรงกับของจริงทุกบรรทัด (LessonView.tsx:151/172/313, SimulationBlock.tsx:88, CheckpointQuiz.tsx:37) ตัวเลข 39 นับซ้ำได้จริง"
    },
    {
      "command": "นับข้อเท็จจริงจากไฟล์จริง (capstone / checkpoint / ไฟล์ภาษาไทย / server-only ใน package.json)",
      "result": "capstone 3 จุด (permissions 5, pipes-and-logs 5, formats-hands-on 3) · lesson ที่มี checkpoint 15 · ไฟล์บทเรียนภาษาไทย 1 ไฟล์ทั้ง repo · server-only ไม่ใช่ dependency · wrangler.jsonc ไม่มี run_worker_first"
    },
    {
      "command": "git diff --check && git status --short",
      "result": "ไม่มี whitespace error · เหลือ 17 PNG ที่ dirty (บันทึกไว้ใน Dirty State)"
    },
    {
      "command": "node session-cleanup.mjs verify --repo <academy-platform> --scratchpad <session> --since 2026-08-01",
      "result": "exit 0 — session-cleanup/v1 clean=true; session residue: 0 paths (0 B reclaimable), 0 processes"
    }
  ],
  "cleanup": {
    "processes": "หยุด next dev บนพอร์ต 3100 (pid 85297) แล้ว · session-cleanup verify รายงาน 0 processes · ไม่มีอะไรตั้งใจปล่อยให้รันต่อ",
    "artifacts": "session-cleanup/v1 clean=true; session residue: 0 paths (0 B reclaimable), 0 processes · ลบ log ชั่วคราวของ RIL ที่ /tmp/ril*.txt เอง 7 ไฟล์ (~2.1 MB) เพราะอยู่นอก scratchpad ที่เกตมองเห็น · ไม่มี --keep"
  }
}
-->

## Objective
ลงมือ implement ตาม plans/implementation-plan-2026-08-02.md ที่ผ่าน RIL 7 รอบ เริ่มที่ W0-0 (โครง attempt + คลังข้อ) ซึ่งเป็นงานแรกที่ทุกอย่างพึ่ง

## Owner Intent And Decisions

**Decisions ที่ founder เคาะแล้ว:**
- D1–D13 ล็อกแล้วใน §3 ของแผน — รวมถึงต้องสมัครบัญชีก่อนใช้ (กัน burn ค่า infra), บัญชีเดียวใช้ได้ทุก product, frontend อยู่บน Cloudflare
- W0-3 ล็อก: บทปกติผ่านเมื่อผิดไม่เกิน 1 ข้อ · retry ไม่จำกัด · และยอมรับว่า completed ไม่ใช่หลักฐาน → น้ำหนักใบรับรองอยู่ที่ capstone ทั้งหมด
- W0-4 ล็อก: คำถามกลางวิดีโอเป็น formative ไม่ใช่ด่าน
- W0-0 ล็อก: ทำคลังข้อเฉพาะ capstone 3 จุด และปิด test-out บน node ที่ยังไม่มีคลังข้อ
- W0-1 ล็อก: /player ไม่แก้ในเฟสนี้ (INTERNAL ONLY ตาม CAS-005) แต่ต้องมีเทสยืนยันว่าเข้าไม่ได้จากภายนอก
- founder: lab ไม่ใช่จุดขายที่สำคัญที่สุด — การที่ video + quiz + capstone + simulation + lab รวมเป็น experience เดียวต่างหากคือจุดขาย จึงไม่กลัวการก๊อปเนื้อหาแบบอ่านช้าๆ
- founder: session identity เป็นตัวตัดสินเรื่อง central login · Academy ปรับตามภายหลัง

**Allowed scope:**
- แก้โค้ดใน academy-web/ ตาม W0-0 → W0-1..4 → W1 → W2 → W3 → W4 ของ plans/implementation-plan-2026-08-02.md
- เขียน migration ใหม่ใน academy-web/supabase/migrations/ (ตาราง attempt + ตัวนับครั้งแบบถาวร)
- เขียนเนื้อหาคลังข้อ capstone ภาษาอังกฤษ (W-content)
- เขียนและรันเทส unit/integration/e2e
- อัปเดต plans/active_plan.md, plans/completed_log.md, PENDING_USER_ACTION.md, SBOM.md
- รัน wrangler dev --remote หรือ preview worker เพื่อ verify /media/*

## Repository State
- State: ready
- Branch: main
- Baseline: ba8814fee0a50b2025a1a9d3ad48089f6bcf2b50
- Delivery: local

## Completed This Session
- craft แผน implement ที่สะท้อน decision/ข้อจำกัด/ข้อเท็จจริงทั้งหมดจากบทสนทนา — plans/implementation-plan-2026-08-02.md
- RIL 7 รอบ สองเลนอิสระ (codex model_reasoning_effort=high + Claude critic) จน PASS ทั้งสองเลนในรอบที่ 7
- พิสูจน์ด้วยการรันจริงว่าคำอ้างเดิม 'แกนหลักฐานต่อครบแล้ว' เป็นเท็จ — พบ F1–F5 ที่ยังอยู่บน HEAD
- แก้ blocking 16 ข้อและข้อเท็จจริงในแผนที่ผิด 8 ข้อ โดยยืนยันซ้ำจากไฟล์จริงทุกข้อก่อนแก้
- บันทึกบทเรียนสองข้อเป็น memory พร้อม prevention control
- ชี้ให้ active_plan.md ไปที่แผนที่ผ่าน RIL แล้ว

### F1–F5 — ข้อบกพร่องที่ยังอยู่บน HEAD (พิสูจน์แล้ว ไม่ใช่ข้อสงสัย)

| # | ข้อบกพร่อง | ที่ |
|---|---|---|
| F1 | เฉลยและคำอธิบายถูกส่งไป browser ทั้งชุด (MCQ · video cue · simulation · `/player`) | `lessons/[nodeId]/page.tsx:35` |
| F2 | บทปกติ "ผ่าน" ด้วยการตอบครบ ไม่ต้องตอบถูก | `api/progress/route.ts:125–127` |
| F3 | ใบรับรองนับ `completed` เป็น "พิสูจน์แล้ว" → ตอบผิดทุกข้อก็ได้ใบ | `roadmap.ts:239` |
| F4 | UI ประกาศว่าผ่านก่อนเซิร์ฟเวอร์ตอบ (ทิ้ง `outcome`) | `LessonView.tsx:151,172` |
| F5 | คำถามกลางวิดีโอตรวจและบันทึกจริง แต่ไม่ถูกใช้ตัดสินอะไร | — |

## Changed Files
- `plans/implementation-plan-2026-08-02.md`
  แผน implement ที่ผ่าน RIL — เอกสารหลักที่ session ถัดไปเดินตาม (commit ba8814f)
- `plans/active_plan.md`
  เพิ่มหัวข้อชี้ไปที่แผนที่ผ่าน RIL พร้อมตาราง F1–F5 และลำดับงานที่บังคับ (commit ba8814f)
- `artifacts/oneshot-2026-07-31/`
  17 PNG ที่ถูก render ใหม่จากการรัน e2e — ยัง dirty ไม่ได้ commit

## Verification
- **codex exec -c model_reasoning_effort=high (RIL รอบที่ 7 บน plans/implementation-plan-2026-08-02.md)**
  → PASS — 'ไม่เหลือ blocker; มีเพียงหัวข้อ W0 ซ้ำหนึ่งครั้ง เป็น editorial เท่านั้น' (แก้แล้ว)
- **Claude critic subagent (RIL รอบที่ 7 — ตรวจการแก้ 6 จุดกับไฟล์จริงและตรวจทุกบรรทัดโค้ดที่แผนอ้างถึงซ้ำ)**
  → PASS — ยืนยันว่าการแก้ทั้ง 6 จุดอยู่ในไฟล์จริงและถูกต้อง และโค้ดที่อ้างตรงกับของจริงทุกบรรทัด (LessonView.tsx:151/172/313, SimulationBlock.tsx:88, CheckpointQuiz.tsx:37) ตัวเลข 39 นับซ้ำได้จริง
- **นับข้อเท็จจริงจากไฟล์จริง (capstone / checkpoint / ไฟล์ภาษาไทย / server-only ใน package.json)**
  → capstone 3 จุด (permissions 5, pipes-and-logs 5, formats-hands-on 3) · lesson ที่มี checkpoint 15 · ไฟล์บทเรียนภาษาไทย 1 ไฟล์ทั้ง repo · server-only ไม่ใช่ dependency · wrangler.jsonc ไม่มี run_worker_first
- **git diff --check && git status --short**
  → ไม่มี whitespace error · เหลือ 17 PNG ที่ dirty (บันทึกไว้ใน Dirty State)
- **node session-cleanup.mjs verify --repo <academy-platform> --scratchpad <session> --since 2026-08-01**
  → exit 0 — session-cleanup/v1 clean=true; session residue: 0 paths (0 B reclaimable), 0 processes

⚠️ **ไม่ได้รัน** `npm run build/lint/test/test:e2e` ใน session นี้ เพราะไม่ได้แก้โค้ดเลย
— session นี้ผลิตเอกสารอย่างเดียว · session ถัดไปต้องรัน full chain ตามปกติ

## Dirty State

Worktree ไม่สะอาด — เหลือ 17 ไฟล์ (mode: allowlisted)

- `M` artifacts/oneshot-2026-07-31/course/course-roadmap-dark-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/course-roadmap-dark-mobile-390.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/course-roadmap-in-progress-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/dashboard-in-progress-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/handouts-and-links-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/lab-inline-dialog-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/lesson-with-video-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/lesson-with-video-mobile-390.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/video-popquiz-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/course/video-popquiz-mobile-390.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/m1/privacy-th-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/m2/pbq-all-kinds-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/m2/pbq-exhibit-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/m2/quiz-mid-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/m2/quiz-mid-mobile-390.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/m2/results-desktop-1440.png — owner: continuation
- `M` artifacts/oneshot-2026-07-31/m2/review-desktop-1440.png — owner: continuation

เหตุผล (ทุกไฟล์เหมือนกัน): ภาพถูก render ใหม่ระหว่างรัน e2e ของ session ก่อนหน้า ต่างกันแค่ byte (git diff รายงาน Bin ... -> ... ทั้ง 17 ไฟล์ 0 insertions 0 deletions) ยังไม่ได้เปรียบเทียบภาพจริงว่าเนื้อหาเปลี่ยนไหม จึงไม่ commit และไม่ restore — session ถัดไปตัดสินได้

## Cleanup State
- Processes: หยุด next dev บนพอร์ต 3100 (pid 85297) แล้ว · session-cleanup verify รายงาน 0 processes · ไม่มีอะไรตั้งใจปล่อยให้รันต่อ
- Artifacts: session-cleanup/v1 clean=true; session residue: 0 paths (0 B reclaimable), 0 processes · ลบ log ชั่วคราวของ RIL ที่ /tmp/ril*.txt เอง 7 ไฟล์ (~2.1 MB) เพราะอยู่นอก scratchpad ที่เกตมองเห็น · ไม่มี --keep

## Remaining Work And Risks

**งานที่เหลือ** — ลำดับบังคับ ห้ามสลับ:
- W0-0: ตาราง attempt + POST /api/attempts + atomic consume ที่มี ownership/context/expiry ใน WHERE เดียว + ตัวนับครั้งแบบถาวร atomic
- W0-0: คลังข้อ capstone EN 39 ข้อ (มี 13 ต้องเขียนเพิ่ม 26) + remap key ต่อ attempt + ปิด test-out บน node ที่ไม่มีคลังข้อ
- W0-1: แยก server-only answer-key model ออกจาก public DTO ทั้ง 4 พื้นผิว + assessed คืน {passed} เท่านั้น + สัญญา practice
- W0-2: UI ต้องรอ outcome จากเซิร์ฟเวอร์ก่อน setDone
- W0-3: certificateEligibility แยกสองเงื่อนไข (ทุก node completed/tested-out และทุก capstone ผ่านแบบ assessed)
- W0-4: แก้เอกสารทุกที่ที่เรียกคำถามกลางวิดีโอว่าแกนหลักฐาน
- W1: ต่อ simulation เข้า checkpoint + เขียน simulation ลง capstone จริงอย่างน้อย 1 จุด
- W2: W2-0 เรื่อง /media/* ก่อน แล้วค่อย guard กลาง authorizeCourseResource + entitlement
- W3: เพิ่ม capstone จุดที่ 3 ใน basic-os-linux + bump version 1.1.0
- W4: ใบรับรองที่ตรวจสอบได้ (หลัง W0+W1+W3)
- W5/W6: bot detection · surface จำลองอันที่สอง
- W-content: บทเรียนภาษาไทย (ทั้ง repo มีไฟล์เดียว)
- หนี้เอกสาร: PENDING_USER_ACTION.md ยังอ้าง Vercel และ migration 0001 อย่างเดียว ทั้งที่มี 0002–0004 และ deploy อยู่บน Cloudflare — แก้ก่อนลงมือ W0

**ความเสี่ยง:**
- W2 มี founder blocker สองตัวที่ยังไม่ได้ขอ: R2 bucket และการอนุญาต deploy หนึ่งครั้งเพื่อ verify /media/* — ต้องเพิ่มลง PENDING_USER_ACTION.md ตั้งแต่เปิด session ไม่ใช่ค้นพบตอนติด
- W0-0 มีงานเนื้อหา 26 ข้อที่ไม่ใช่งานโค้ด — ถ้าไม่แยกนับจะกลายเป็นของค้างที่บล็อกทุกอย่างข้างหลัง
- โควตา 3 ครั้ง/30 นาที ผูกกับบัญชี ซึ่งเลี่ยงได้ด้วยการสมัครใหม่ (D1 เปิดสมัครเสรี) — เป็นแค่ speed bump จนกว่า W5 จะทำให้การสมัครมีต้นทุน
- e2e ที่รันผ่าน npm run start จะเขียวปลอมสำหรับ /media/* เพราะไม่มี ASSETS binding
- simulation checkpoint พิสูจน์แค่ว่า 'รู้คำตอบที่ถูก' ไม่ใช่ 'ลงมือทำจริง' — ห้ามเคลมเกินนี้บนหน้าเว็บหรือบนใบรับรอง
- session identity อาจเคาะให้มี central sign-in ซึ่งจะทำให้ /sign-in กลายเป็น redirect — อย่าลงแรงกับหน้านั้นเพิ่ม

No blocker.

## Exact Next Action

Working directory: academy-web

เริ่ม W0-0: สร้างตาราง attempt + endpoint POST /api/attempts + atomic consume แล้วต่อด้วยคลังข้อ capstone

First step: อ่าน plans/implementation-plan-2026-08-02.md §0 (F1–F5) และ §5 W0-0 ให้ครบ แล้วแก้ PENDING_USER_ACTION.md ให้ตรงความจริง (Vercel → Cloudflare, migration 0001 → 0001–0004) พร้อมเพิ่มสองรายการใน §4.2b (R2 bucket + ขออนุญาต deploy verify /media/*) จากนั้นเขียน migration 0005 สร้างตาราง academy.attempt ตาม field ในตารางของ W0-0

Commands:
- cd academy-web
- npm run build && npm run lint && npm run test && npm run test:e2e

## Done Definition
- migration 0005 สร้างตาราง academy.attempt ครบทุก field ใน W0-0 (attempt_id, user_id, course_slug, node_id, challenge_id, params, challenge_version, expires_at, consumed_at) และ RLS เปิดแบบ default deny เหมือนตารางอื่นในสคีมา
- POST /api/attempts ออก attempt ที่เดาไม่ได้ และผูกกับผู้ใช้ที่ล็อกอินเท่านั้น
- consume เป็นคำสั่งเดียวที่มี attempt_id + user_id + course_slug + node_id + challenge_id + consumed_at is null + expires_at > now() อยู่ใน WHERE เดียวกัน
- เทส race: ยิง attempt_id เดียวกันพร้อมกันสองเส้น → ผ่านได้อย่างมากหนึ่งเส้น
- เทส: ใช้ attempt_id ของผู้ใช้คนอื่น / ของ node อื่น / ที่หมดอายุ → ถูกปฏิเสธ
- npm run build && npm run lint && npm run test && npm run test:e2e เขียวทั้งหมด

## Do Not Touch
- deploy ขึ้น production หรือ push โดยไม่ได้รับอนุญาตจาก founder ในรอบนั้น
- สร้าง R2 bucket หรือแตะ external service ใดๆ เอง (Supabase console, Cloudflare dashboard, DNS)
- รื้อ decision D1–D13 ใน §3 ของแผนโดยไม่ถาม founder
- แก้ระบบ Identity Control หรือเปิด /sign-in — session identity เป็นเจ้าของ (§6 ของแผน)
- ดึงงานภาษาไทย (W-content) เข้ามาเป็นเกณฑ์ปิด W0
- แตะไฟล์ใน artifacts/oneshot-2026-07-31/ นอกจากจะตัดสินใจเรื่อง 17 ไฟล์ที่ dirty อยู่

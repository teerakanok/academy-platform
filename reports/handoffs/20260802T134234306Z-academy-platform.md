# Session Handoff: academy-platform

<!-- session-handoff/v1
{
  "schema": "session-handoff/v1",
  "id": "20260802T134234306Z-academy-platform",
  "created_at": "2026-08-02T13:42:34.306Z",
  "project": "academy-platform",
  "objective": "ปิดวงจรหลักฐานของ capstone ให้เชื่อถือได้ แล้วเดินต่อไปที่ W2 (สิทธิ์เข้าถึง + /media)",
  "state": "blocked",
  "repo": {
    "remote": "github.com/teerakanok/academy-platform",
    "branch": "main",
    "base_head": "842b60756cedff065f52376da7930729eb397fec"
  },
  "delivery": "local",
  "worktree": {
    "mode": "clean",
    "entries": []
  },
  "scope": {
    "allowed": [
      "แก้โค้ด/เทส/migration ภายใน products/cyberskills/academy-platform",
      "รัน build, lint, vitest, playwright, supabase db reset กับ local stack",
      "commit ในรีโป academy-platform (ไม่ push)",
      "อ่านสถานะ Pool A ผ่านเอกสาร reports/state/ ของ director"
    ],
    "forbidden": [
      "แก้ไฟล์ใน products/cyberskills/identity-control (งาน session อื่น อยู่ระหว่างทำ)",
      "push ไป remote ใดๆ (43+ commits ยังไม่ push โดยตั้งใจ)",
      "deploy Worker หรือสร้าง R2 bucket โดยไม่ได้ยืนยันกับเจ้าของในเซสชันใหม่",
      "รัน migration บน Pool A หรือแก้ PGRST_DB_SCHEMAS โดยไม่ได้ยืนยันสิทธิ์ในเซสชันใหม่",
      "commit pointer ของ academy ในรีโป director (guard ฟ้องไฟล์ของ identity-control อยู่)"
    ]
  },
  "canonical_read_order": [
    "AGENTS.md",
    "plans/implementation-plan-2026-08-02.md",
    "plans/active_plan.md",
    "reports/reviews/ril-attempt-lifecycle-2026-08-02.md",
    "PENDING_USER_ACTION.md"
  ],
  "owner_decisions": [
    "2026-08-02 อนุมัติ: สร้างสคีมา academy บน Pool A และงานที่เกี่ยวข้องบน Pool A",
    "2026-08-02 อนุมัติ: R2 bucket + deploy หนึ่งครั้งเพื่อ verify /media/*",
    "2026-08-02 เลือก: ตัวกวาด attempt ใช้ Cloudflare Cron Trigger (ไม่ใช่ pg_cron ไม่ใช่สั่งมือ)",
    "2026-08-02 ยังไม่ตัดสิน: จะเปิดระบบบัญชีบน production preview เมื่อไร (ผู้ช่วยเสนอให้ยังไม่เปิดจนกว่า identity contract จะลงตัว)",
    "2026-08-02 ยังไม่ตัดสิน: จะใช้ least-privilege role เฉพาะสคีมา academy หรือใช้ service_role ร่วมไปก่อน (ผู้ช่วยแนะนำอย่างแรก เจ้าของยังไม่ระบุ)"
  ],
  "completed": [
    "W1d — โจทย์จำลองสุ่มค่าเป้าหมายต่อ attempt และผูกกับ attempt ตัวเอง (1467710)",
    "ปิด 3 blocker + 2 should-fix จาก RIL รอบ W1 (77a14f2) — oracle ที่ GET, ปุ่มลองใหม่ตัน, แม่แบบที่ยังไม่แทนค่าถูกเอาไปตรวจ, reset ลบสมุดนับโควตา, ออก attempt ทั้งที่ทำจบแล้ว",
    "W0-0b — ต่อสาย remap key ให้มีผลจริง (78346b0) — เดิม /api/attempts ออก MCQ ที่ remap แล้วแต่ไม่มีใครใช้",
    "ปิด blocker + 3 ข้อจาก RIL รอบ 2 (da68a29) — ชุด key จริงยังหลุดไปกับหน้า, /api/explanations คืน key เฉลย, issuer ใช้คนละเกณฑ์, 409 แล้วตัน",
    "migration 0008 — บันทึกว่าผ่านด้วย attempt ไหน/กติกาเวอร์ชันไหน + attempt ถือโจทย์ทั้งชิ้น (5128b83)",
    "migration 0009 — ส่งคำตอบแล้วระบบล้มกลางทางต้องไม่กินสิทธิ์ (bbea1b4)",
    "migration 0010 — เปิดหน้าซ้ำได้โจทย์ใบเดิม ไม่กินโควตาใหม่ (522d314)",
    "migration 0011 + PENDING §7b — ตัวกวาด attempt ที่หมดอายุ (112c03b)",
    "Cloudflare Cron Trigger + worker.ts + tsconfig.worker.json (1dc3de9) — ยืนยันด้วย wrangler dev --test-scheduled กับ Postgres จริง",
    "migration 0012 — ปิด 3 ข้อที่ RIL รอบ 3 สองเลนชี้ตรงกัน + 2 ข้อจาก red-team (347660b)",
    "scripts/mutation-check.sh — พังโค้ดพิสูจน์เทสแล้วคืนจากสำเนา (57c5ee6) หลังเหตุ git checkout ลบงานที่ยังไม่ commit",
    "reports/reviews/ril-attempt-lifecycle-2026-08-02.md — เก็บรายงาน RIL รอบ 3 ทั้งสองเลนไว้ในรีโป (842b607)"
  ],
  "changed_files": [
    { "path": "academy-web/supabase/migrations/0008_passing_attempt.sql", "reason": "ตัวชี้ว่าผ่านด้วย attempt/เวอร์ชันไหน เขียนได้ครั้งเดียว ไม่ผูก FK เพราะ retention ต้องกวาดได้" },
    { "path": "academy-web/supabase/migrations/0009_attempt_finalize.sql", "reason": "attempt.outcome + finalize_attempt + กติกายึดซ้ำเมื่อค้างเกิน 30 วินาที" },
    { "path": "academy-web/supabase/migrations/0010_reuse_active_attempt.sql", "reason": "issue_attempt คืนใบที่ยังไม่ถูกใช้ และคืน params ที่เก็บอยู่จริง" },
    { "path": "academy-web/supabase/migrations/0011_attempt_retention.sql", "reason": "purge_expired_attempts" },
    { "path": "academy-web/supabase/migrations/0012_attempt_integrity_fixes.sql", "reason": "ต่ออายุ claim + แช่แข็งหลักฐานเมื่อผ่านแล้ว + ตัวกวาดเว้นใบที่เป็นหลักฐาน" },
    { "path": "academy-web/src/app/api/progress/route.ts", "reason": "ตรวจจาก snapshot ของ attempt ล้วน + ปิด attempt ทุกทางออกที่ปฏิเสธหลัง consume" },
    { "path": "academy-web/src/app/api/attempts/route.ts", "reason": "ตอบจาก params ที่เก็บจริง + ใช้ requiresAttempt เกณฑ์เดียวกับที่อื่น" },
    { "path": "academy-web/src/lib/course/attempt-grading.ts", "reason": "จุดเดียวที่ตัดสินว่าตรวจด้วยโจทย์ชุดไหน (แยกออกมาเพื่อให้เทสตรึงของจริงได้)" },
    { "path": "academy-web/src/lib/course/public-progress.ts", "reason": "ตัดผลรายข้อของพื้นผิววัดผลออกจากทุกเส้นทางที่ส่งไป browser" },
    { "path": "academy-web/src/components/course/use-lesson-attempt.ts", "reason": "state machine ของ attempt ฝั่ง UI (รอ/พร้อม/ขอไม่ได้)" },
    { "path": "academy-web/worker.ts", "reason": "Worker entry ที่ห่อ OpenNext handler แล้วเพิ่ม scheduled สำหรับ cron" },
    { "path": "academy-web/scripts/mutation-check.sh", "reason": "พังโค้ดพิสูจน์เทสแล้วคืนจากสำเนา ไม่ใช่ git checkout" }
  ],
  "remaining_work": [
    "🔒 ต้องยืนยันก่อน: สร้างสคีมา academy บน Pool A + รัน migration 0001-0012 + เพิ่ม academy เข้า PGRST_DB_SCHEMAS + reload PostgREST",
    "🔒 ต้องยืนยันก่อน: ตัดสินใจเรื่อง least-privilege role แล้วตั้ง secret ของ Worker (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)",
    "🔒 ต้องยืนยันก่อน: สร้าง R2 bucket + ย้าย /media ออกจาก public/ + deploy หนึ่งครั้งเพื่อ verify",
    "✅ ทำได้เลย: #64 คำตอบที่พิมพ์ไว้ยังหายเมื่อ refresh (เก็บใน sessionStorage ผูกกับ attemptId)",
    "✅ ทำได้เลย: W2 ส่วน local — authorizeCourseResource() guard กลาง + inventory ทุกพื้นผิวที่ปล่อยทรัพยากร",
    "✅ ทำได้เลย: W-content — คลังข้อ capstone EN 39 ข้อ (เงื่อนไขเดียวที่เหลือของการเปิด test-out และของการปิดช่องบอกต่อคำตอบ)",
    "✅ ทำได้เลย: W3 (capstone ตัวที่สาม) ตามแผน implementation §5"
  ],
  "risks": [
    "วิดีโอบทเรียนบน production โหลดได้โดยไม่ต้องมีบัญชี — /media/* เสิร์ฟผ่าน ASSETS binding request ไม่ถึง Worker และ middleware แก้ไม่ได้ (นี่คือรูที่เปิดอยู่จริงวันนี้)",
    "migration 0001-0012 อยู่แค่ในเครื่อง — Pool A ยังไม่มีสคีมา academy ตามเอกสาร 2026-08-01 ต้องยืนยันกับเซิร์ฟเวอร์จริงก่อนลงมือ",
    "SUPABASE_SERVICE_ROLE_KEY ของ Pool A ข้าม RLS ทุกสคีมา (crucible/billibella/phalanx/angler/forge/star/orion/helm) — วางบน Worker ของ Academy = Academy กลายเป็นจุดเดียวที่ทำให้ 8 product เสียหายพร้อมกัน",
    "การบอกต่อ 'ข้อความ' ของตัวเลือกที่ถูกยังทำได้ จนกว่าคลังข้อจะโตพอให้หมุนจริง (W-content) — remap กันได้แค่การบอกต่อตัวอักษร",
    "ด่านจำลองพิสูจน์แค่ 'ส่งสถานะปลายทางที่ถูก' ไม่ใช่ 'ลงมือทำ' — W4 ต้องไม่อ้างเกินนี้ในใบรับรอง",
    "pointer ของ academy ในรีโป director commit ไม่ได้ เพราะ scripts/check-shared-infra-docs.sh ฟ้องไฟล์ของ identity-control (งาน session อื่น) — ห้ามแก้ไฟล์นั้นเอง",
    "e2e ทั้งชุดใช้บัญชีเดียวและรันด้วย ATTEMPT_MAX_PER_WINDOW=500 — ขอบเขตโควตาจริงถูกพิสูจน์ที่ integration เท่านั้น",
    "เคยเห็น e2e proof-not-progress แดงหนึ่งครั้งในการรันเต็มชุด แล้วเขียวสองรอบถัดมาโดยไม่แก้อะไร — อาจเป็น flake ที่ยังไม่ได้ไล่"
  ],
  "next": {
    "cwd": "academy-web",
    "summary": "สร้างสคีมา academy บน Pool A + รัน migration 0001-0012 + เพิ่มเข้า PGRST_DB_SCHEMAS (เจ้าของอนุมัติในเซสชันก่อน ต้องยืนยันซ้ำ) แล้วต่อด้วย least-privilege role และ R2 + /media gating",
    "first_step": "อ่าน ecosystem/SHARED_INFRA_ACCESS.md และ reports/state/supabase.md แล้วยืนยันกับเจ้าของว่ายังอนุมัติงานบน Pool A อยู่ จากนั้นตรวจสถานะจริงบนเซิร์ฟเวอร์ก่อนแตะอะไร (สคีมา academy มีอยู่แล้วหรือยัง PGRST_DB_SCHEMAS ปัจจุบันคืออะไร)",
    "commands": [
      "cat ../../../ecosystem/SHARED_INFRA_ACCESS.md",
      "sed -n '18,40p' ../../../reports/state/supabase.md",
      "ls academy-web/supabase/migrations/",
      "npx supabase db reset --local && npx vitest run   # ยืนยันว่า migration ทั้งชุดยังสะอาดในเครื่องก่อนขึ้น Pool A"
    ],
    "acceptance": [
      "สคีมา academy มีอยู่บน Pool A พร้อม migration 0001-0012 ครบ ตรวจได้จากรายชื่อตาราง/ฟังก์ชันจริง",
      "PGRST_DB_SCHEMAS มี academy และ PostgREST reload แล้ว (ยิง REST จริงได้)",
      "reports/state/supabase.md อัปเดตให้ตรงกับของจริงหลังทำ",
      "ยังไม่ตั้ง secret ของ Worker จนกว่าจะตัดสินเรื่อง least-privilege role"
    ],
    "execution_boundary": "blocked-external-or-sensitive"
  },
  "blocker": {
    "reason": "งานถัดไปแตะ Pool A ซึ่งเป็นฐานข้อมูล production ที่ใช้ร่วมกับอีก 8 product และต้องใช้ credential ของเซิร์ฟเวอร์ · เจ้าของอนุมัติปากเปล่าในเซสชัน 2026-08-02 แล้ว แต่ handoff เป็นหลักฐาน ไม่ใช่ใบอนุญาต จึงส่งต่อการอนุมัตินั้นข้ามเซสชันไม่ได้",
    "required_input": "ยืนยันอีกครั้งว่าให้ดำเนินการบน Pool A ได้ (สร้างสคีมา academy + รัน migration + แก้ PGRST_DB_SCHEMAS + reload) และเลือกระหว่าง (ก) สร้าง Postgres role เฉพาะสคีมา academy แล้วออก JWT ของ role นั้น หรือ (ข) ใช้ service_role ร่วมไปก่อน · ถ้ายังไม่พร้อม ให้ข้ามไปทำงานในรายการ '✅ ทำได้เลย' แทน"
  },
  "verification": [
    { "command": "npm run build", "result": "Compiled successfully" },
    { "command": "npm run lint", "result": "0 errors (เหลือ 1 warning เดิม: unused eslint-disable ใน registry.generated.ts) · lint ตรวจทั้ง tsconfig หลักและ tsconfig.worker.json" },
    { "command": "npx vitest run", "result": "347 passed (unit + integration + types) · Type Errors: none" },
    { "command": "npm run test:e2e", "result": "111 passed, 10 skipped" },
    { "command": "scripts/mutation-check.sh (11 ครั้งตลอด session)", "result": "แดงตามคาดทุกครั้ง ยกเว้นหนึ่งครั้งที่รอด → แก้เทส reload ให้เทียบเนื้อโจทย์แทน id แล้วแดง" },
    { "command": "wrangler dev --test-scheduled + curl /__scheduled", "result": "log 'ลบ 1 แถว ใน 2 รอบ' และแถวเก่าหายจาก academy.attempt จริง (ตรวจซ้ำด้วย psql)" },
    { "command": "node session-cleanup.mjs verify", "result": "clean=true; session residue: 0 paths (0 B reclaimable), 0 processes" }
  ],
  "cleanup": {
    "processes": "session-cleanup/v1 clean=true; session residue: 0 paths (0 B reclaimable), 0 processes — ไม่มี dev server, wrangler, หรือ codex ของเซสชันนี้ค้าง (เคยเห็น Chrome headless และ codex exec ที่ยังรัน แต่ตรวจ parent แล้วเป็นของ ChatGPT.app และของ session Crucible อื่น จึงไม่แตะ)",
    "artifacts": "scratchpad ถูกกวาดทั้งหมด 15.2 MB โดย sweep --apply · รายงาน RIL รอบ 3 ทั้งสองเลนถูกย้ายเข้ารีโปก่อนกวาดที่ reports/reviews/ril-attempt-lifecycle-2026-08-02.md · ไม่มี --keep"
  }
}
-->

## Objective
ปิดวงจรหลักฐานของ capstone ให้เชื่อถือได้ แล้วเดินต่อไปที่ W2 (สิทธิ์เข้าถึง + /media)

## Owner Intent And Decisions
- Allowed scope: แก้โค้ด/เทส/migration ภายใน products/cyberskills/academy-platform
- Allowed scope: รัน build, lint, vitest, playwright, supabase db reset กับ local stack
- Allowed scope: commit ในรีโป academy-platform (ไม่ push)
- Allowed scope: อ่านสถานะ Pool A ผ่านเอกสาร reports/state/ ของ director
- Forbidden: แก้ไฟล์ใน products/cyberskills/identity-control (งาน session อื่น อยู่ระหว่างทำ)
- Forbidden: push ไป remote ใดๆ (43+ commits ยังไม่ push โดยตั้งใจ)
- Forbidden: deploy Worker หรือสร้าง R2 bucket โดยไม่ได้ยืนยันกับเจ้าของในเซสชันใหม่
- Forbidden: รัน migration บน Pool A หรือแก้ PGRST_DB_SCHEMAS โดยไม่ได้ยืนยันสิทธิ์ในเซสชันใหม่
- Forbidden: commit pointer ของ academy ในรีโป director (guard ฟ้องไฟล์ของ identity-control อยู่)
- 2026-08-02 อนุมัติ: สร้างสคีมา academy บน Pool A และงานที่เกี่ยวข้องบน Pool A
- 2026-08-02 อนุมัติ: R2 bucket + deploy หนึ่งครั้งเพื่อ verify /media/*
- 2026-08-02 เลือก: ตัวกวาด attempt ใช้ Cloudflare Cron Trigger (ไม่ใช่ pg_cron ไม่ใช่สั่งมือ)
- 2026-08-02 ยังไม่ตัดสิน: จะเปิดระบบบัญชีบน production preview เมื่อไร (ผู้ช่วยเสนอให้ยังไม่เปิดจนกว่า identity contract จะลงตัว)
- 2026-08-02 ยังไม่ตัดสิน: จะใช้ least-privilege role เฉพาะสคีมา academy หรือใช้ service_role ร่วมไปก่อน (ผู้ช่วยแนะนำอย่างแรก เจ้าของยังไม่ระบุ)

บริบทเพิ่มเติม:
- เจ้าของสั่งยืนหนึ่งตลอดเซสชัน: โค้ดต้อง maintainable ไม่มี spaghetti และ **RIL ทุก
  milestone สำคัญ** ครอบ ux-ui / security / performance / cost — เซสชันนี้ทำครบสามรอบ
  รอบสุดท้ายสองเลน (reviewer + red-team)
- เหตุผลที่เสนอให้ยังไม่เปิดบัญชีบน preview: ถ้าเปิดตอนนี้จะมีอีเมลจริงลง auth ของ Pool A
  ซึ่งชนกับแผน universal identity (`accounts.cyberskills.co.th` เป็น issuer กลาง)
  อาจต้องย้ายบัญชีผู้ใช้จริงทีหลัง · ด้าน "ไม่ล็อกอิน = โหลดสื่อไม่ได้" ทดสอบได้โดยไม่ต้องเปิด

## Repository State
- State: blocked
- Branch: main
- Baseline: 842b60756cedff065f52376da7930729eb397fec
- Delivery: local
- Ahead of origin: 43+ commits (ยังไม่ push โดยตั้งใจ — การ push ต้องขออนุญาตแยก)

## Completed This Session
- W1d — โจทย์จำลองสุ่มค่าเป้าหมายต่อ attempt และผูกกับ attempt ตัวเอง (1467710)
- ปิด 3 blocker + 2 should-fix จาก RIL รอบ W1 (77a14f2) — oracle ที่ GET, ปุ่มลองใหม่ตัน, แม่แบบที่ยังไม่แทนค่าถูกเอาไปตรวจ, reset ลบสมุดนับโควตา, ออก attempt ทั้งที่ทำจบแล้ว
- W0-0b — ต่อสาย remap key ให้มีผลจริง (78346b0) — เดิม /api/attempts ออก MCQ ที่ remap แล้วแต่ไม่มีใครใช้
- ปิด blocker + 3 ข้อจาก RIL รอบ 2 (da68a29) — ชุด key จริงยังหลุดไปกับหน้า, /api/explanations คืน key เฉลย, issuer ใช้คนละเกณฑ์, 409 แล้วตัน
- migration 0008 — บันทึกว่าผ่านด้วย attempt ไหน/กติกาเวอร์ชันไหน + attempt ถือโจทย์ทั้งชิ้น (5128b83)
- migration 0009 — ส่งคำตอบแล้วระบบล้มกลางทางต้องไม่กินสิทธิ์ (bbea1b4)
- migration 0010 — เปิดหน้าซ้ำได้โจทย์ใบเดิม ไม่กินโควตาใหม่ (522d314)
- migration 0011 + PENDING §7b — ตัวกวาด attempt ที่หมดอายุ (112c03b)
- Cloudflare Cron Trigger + worker.ts + tsconfig.worker.json (1dc3de9) — ยืนยันด้วย wrangler dev --test-scheduled กับ Postgres จริง
- migration 0012 — ปิด 3 ข้อที่ RIL รอบ 3 สองเลนชี้ตรงกัน + 2 ข้อจาก red-team (347660b)
- scripts/mutation-check.sh — พังโค้ดพิสูจน์เทสแล้วคืนจากสำเนา (57c5ee6) หลังเหตุ git checkout ลบงานที่ยังไม่ commit
- reports/reviews/ril-attempt-lifecycle-2026-08-02.md — เก็บรายงาน RIL รอบ 3 ทั้งสองเลนไว้ในรีโป (842b607)

**บทเรียนที่บันทึกเป็น memory แล้ว:** สามข้อของ RIL รอบ 3 คือ "ตั้งกติกาแล้วบังคับไม่ครบ
พี่น้องของฟิลด์นั้น" และเทสที่เขียนเองก็ตรวจแค่ครึ่งที่คิดถึง — เทสข้อหนึ่งยืนยันพฤติกรรม
**ผิด**ด้วยซ้ำ · อีกบทเรียน: `git checkout` เพื่อคืนไฟล์หลัง mutation ลบงานที่ยังไม่ commit
ทิ้ง (กู้จาก transcript ได้) → มี `scripts/mutation-check.sh` เป็น prevention control แล้ว

## Changed Files
- academy-web/supabase/migrations/0008_passing_attempt.sql: ตัวชี้ว่าผ่านด้วย attempt/เวอร์ชันไหน เขียนได้ครั้งเดียว ไม่ผูก FK เพราะ retention ต้องกวาดได้
- academy-web/supabase/migrations/0009_attempt_finalize.sql: attempt.outcome + finalize_attempt + กติกายึดซ้ำเมื่อค้างเกิน 30 วินาที
- academy-web/supabase/migrations/0010_reuse_active_attempt.sql: issue_attempt คืนใบที่ยังไม่ถูกใช้ และคืน params ที่เก็บอยู่จริง
- academy-web/supabase/migrations/0011_attempt_retention.sql: purge_expired_attempts
- academy-web/supabase/migrations/0012_attempt_integrity_fixes.sql: ต่ออายุ claim + แช่แข็งหลักฐานเมื่อผ่านแล้ว + ตัวกวาดเว้นใบที่เป็นหลักฐาน
- academy-web/src/app/api/progress/route.ts: ตรวจจาก snapshot ของ attempt ล้วน + ปิด attempt ทุกทางออกที่ปฏิเสธหลัง consume
- academy-web/src/app/api/attempts/route.ts: ตอบจาก params ที่เก็บจริง + ใช้ requiresAttempt เกณฑ์เดียวกับที่อื่น
- academy-web/src/lib/course/attempt-grading.ts: จุดเดียวที่ตัดสินว่าตรวจด้วยโจทย์ชุดไหน (แยกออกมาเพื่อให้เทสตรึงของจริงได้)
- academy-web/src/lib/course/public-progress.ts: ตัดผลรายข้อของพื้นผิววัดผลออกจากทุกเส้นทางที่ส่งไป browser
- academy-web/src/components/course/use-lesson-attempt.ts: state machine ของ attempt ฝั่ง UI (รอ/พร้อม/ขอไม่ได้)
- academy-web/worker.ts: Worker entry ที่ห่อ OpenNext handler แล้วเพิ่ม scheduled สำหรับ cron
- academy-web/scripts/mutation-check.sh: พังโค้ดพิสูจน์เทสแล้วคืนจากสำเนา ไม่ใช่ git checkout

## Verification
- npm run build: Compiled successfully
- npm run lint: 0 errors (เหลือ 1 warning เดิม: unused eslint-disable ใน registry.generated.ts) · lint ตรวจทั้ง tsconfig หลักและ tsconfig.worker.json
- npx vitest run: 347 passed (unit + integration + types) · Type Errors: none
- npm run test:e2e: 111 passed, 10 skipped
- scripts/mutation-check.sh (11 ครั้งตลอด session): แดงตามคาดทุกครั้ง ยกเว้นหนึ่งครั้งที่รอด → แก้เทส reload ให้เทียบเนื้อโจทย์แทน id แล้วแดง
- wrangler dev --test-scheduled + curl /__scheduled: log 'ลบ 1 แถว ใน 2 รอบ' และแถวเก่าหายจาก academy.attempt จริง (ตรวจซ้ำด้วย psql)
- node session-cleanup.mjs verify: clean=true; session residue: 0 paths (0 B reclaimable), 0 processes

## Dirty State
Expected worktree: clean.

`git status --short` ว่างเปล่า · `git diff --check` ผ่าน · ไม่มีไฟล์ที่ตั้งใจทิ้งไว้ให้ทำต่อ

## Cleanup State
- Processes: session-cleanup/v1 clean=true; session residue: 0 paths (0 B reclaimable), 0 processes — ไม่มี dev server, wrangler, หรือ codex ของเซสชันนี้ค้าง (เคยเห็น Chrome headless และ codex exec ที่ยังรัน แต่ตรวจ parent แล้วเป็นของ ChatGPT.app และของ session Crucible อื่น จึงไม่แตะ)
- Artifacts: scratchpad ถูกกวาดทั้งหมด 15.2 MB โดย sweep --apply · รายงาน RIL รอบ 3 ทั้งสองเลนถูกย้ายเข้ารีโปก่อนกวาดที่ reports/reviews/ril-attempt-lifecycle-2026-08-02.md · ไม่มี --keep

## Remaining Work And Risks
- Remaining: 🔒 ต้องยืนยันก่อน: สร้างสคีมา academy บน Pool A + รัน migration 0001-0012 + เพิ่ม academy เข้า PGRST_DB_SCHEMAS + reload PostgREST
- Remaining: 🔒 ต้องยืนยันก่อน: ตัดสินใจเรื่อง least-privilege role แล้วตั้ง secret ของ Worker (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
- Remaining: 🔒 ต้องยืนยันก่อน: สร้าง R2 bucket + ย้าย /media ออกจาก public/ + deploy หนึ่งครั้งเพื่อ verify
- Remaining: ✅ ทำได้เลย: #64 คำตอบที่พิมพ์ไว้ยังหายเมื่อ refresh (เก็บใน sessionStorage ผูกกับ attemptId)
- Remaining: ✅ ทำได้เลย: W2 ส่วน local — authorizeCourseResource() guard กลาง + inventory ทุกพื้นผิวที่ปล่อยทรัพยากร
- Remaining: ✅ ทำได้เลย: W-content — คลังข้อ capstone EN 39 ข้อ (เงื่อนไขเดียวที่เหลือของการเปิด test-out และของการปิดช่องบอกต่อคำตอบ)
- Remaining: ✅ ทำได้เลย: W3 (capstone ตัวที่สาม) ตามแผน implementation §5
- Risk: วิดีโอบทเรียนบน production โหลดได้โดยไม่ต้องมีบัญชี — /media/* เสิร์ฟผ่าน ASSETS binding request ไม่ถึง Worker และ middleware แก้ไม่ได้ (นี่คือรูที่เปิดอยู่จริงวันนี้)
- Risk: migration 0001-0012 อยู่แค่ในเครื่อง — Pool A ยังไม่มีสคีมา academy ตามเอกสาร 2026-08-01 ต้องยืนยันกับเซิร์ฟเวอร์จริงก่อนลงมือ
- Risk: SUPABASE_SERVICE_ROLE_KEY ของ Pool A ข้าม RLS ทุกสคีมา (crucible/billibella/phalanx/angler/forge/star/orion/helm) — วางบน Worker ของ Academy = Academy กลายเป็นจุดเดียวที่ทำให้ 8 product เสียหายพร้อมกัน
- Risk: การบอกต่อ 'ข้อความ' ของตัวเลือกที่ถูกยังทำได้ จนกว่าคลังข้อจะโตพอให้หมุนจริง (W-content) — remap กันได้แค่การบอกต่อตัวอักษร
- Risk: ด่านจำลองพิสูจน์แค่ 'ส่งสถานะปลายทางที่ถูก' ไม่ใช่ 'ลงมือทำ' — W4 ต้องไม่อ้างเกินนี้ในใบรับรอง
- Risk: pointer ของ academy ในรีโป director commit ไม่ได้ เพราะ scripts/check-shared-infra-docs.sh ฟ้องไฟล์ของ identity-control (งาน session อื่น) — ห้ามแก้ไฟล์นั้นเอง
- Risk: e2e ทั้งชุดใช้บัญชีเดียวและรันด้วย ATTEMPT_MAX_PER_WINDOW=500 — ขอบเขตโควตาจริงถูกพิสูจน์ที่ integration เท่านั้น
- Risk: เคยเห็น e2e proof-not-progress แดงหนึ่งครั้งในการรันเต็มชุด แล้วเขียวสองรอบถัดมาโดยไม่แก้อะไร — อาจเป็น flake ที่ยังไม่ได้ไล่

### ขยายความ

### 🔒 ต้องได้รับการยืนยันก่อนลงมือ
1. **สคีมา `academy` บน Pool A** — สร้างสคีมา + รัน migration 0001–0012 + เพิ่ม `academy`
   เข้า `PGRST_DB_SCHEMAS` + reload PostgREST · ตามเอกสาร `reports/state/supabase.md`
   (2026-08-01) Pool A ยังไม่มีสคีมานี้ **แต่เอกสารเป็น snapshot — ต้องตรวจของจริงก่อน**
2. **กุญแจของ Worker** — ตัดสินเรื่อง least-privilege role ก่อน แล้วค่อยตั้ง
   `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` · ตอนนี้ Worker ไม่มี secret เลย
   (`wrangler secret list` → ว่าง) ทุกฟีเจอร์ที่ใช้ DB จึงปิดอยู่ตามที่ตั้งใจ
3. **R2 + `/media/*`** — สร้าง bucket (บัญชีมี R2 ใช้อยู่แล้ว: `crux-backups`,
   `cyberskills-backups`) · ย้ายสื่อออกจาก `public/` · route ออก signed URL ·
   deploy หนึ่งครั้งเพื่อ verify (e2e ในเครื่องพิสูจน์เรื่องนี้ไม่ได้ เพราะ `next start`
   ไม่มี ASSETS binding → เขียวเสมอไม่ว่าถูกหรือผิด)

### ✅ ทำได้เลยโดยไม่ต้องรอใคร
- **#64** คำตอบที่พิมพ์ไว้หายเมื่อ refresh — โจทย์กลับมาใบเดิมแล้ว (0010) เหลือเก็บคำตอบ
  ผูกกับ `attemptId` ใน sessionStorage แล้วล้างเมื่อ consume/หมดอายุ/ออกจากระบบ
- **W2 ส่วน local** — `authorizeCourseResource()` guard กลาง + inventory ทุกพื้นผิวที่ปล่อย
  ทรัพยากร (หน้า lesson, `/api/progress` ทุก action, `/api/progress/reset`, `/api/attempts`,
  `/player/*`, `/media/*`) ตามแผน §5 W2
- **W-content** — คลังข้อ capstone EN 39 ข้อ · เป็นเงื่อนไขเดียวที่เหลือของการเปิด
  `test-out` กลับมา **และ**เป็นสิ่งเดียวที่ปิดช่อง "บอกต่อข้อความคำตอบ" ได้จริง
- **W3** — capstone ตัวที่สามตามแผน

### ความเสี่ยงที่ต้องรู้
- 🔴 **วิดีโอบทเรียนบน production โหลดได้โดยไม่ต้องมีบัญชี** — `/media/*` เสิร์ฟผ่าน ASSETS
  binding request ไม่ถึง Worker เลย และ middleware แก้ไม่ได้เพราะไม่เคยถูกเรียก
- `SUPABASE_SERVICE_ROLE_KEY` ของ Pool A ข้าม RLS ทุกสคีมา (crucible, billibella, phalanx,
  angler, forge, star, orion, helm) — วางบน Worker ของ Academy = Academy กลายเป็นจุดเดียว
  ที่ทำให้ 8 product เสียหายพร้อมกัน
- การบอกต่อ **ข้อความ** ของตัวเลือกที่ถูกยังทำได้จนกว่าคลังข้อจะโตพอให้หมุนจริง
- ด่านจำลองพิสูจน์แค่ "ส่งสถานะปลายทางที่ถูก" ไม่ใช่ "ลงมือทำ" — W4 ต้องไม่อ้างเกินนี้
- pointer ของ academy ในรีโป director commit ไม่ได้ เพราะ `scripts/check-shared-infra-docs.sh`
  ฟ้อง `products/cyberskills/identity-control/AGENTS.md` และ `plans/active_plan.md`
  ว่าอ้าง Pool A/Pool B โดยไม่ลิงก์ `SHARED_INFRA_ACCESS.md` — **ไฟล์ของ session อื่น ห้ามแก้เอง**
- e2e ทั้งชุดใช้บัญชีเดียวและรันด้วย `ATTEMPT_MAX_PER_WINDOW=500` — ขอบเขตโควตาจริง
  ถูกพิสูจน์ที่ integration เท่านั้น (ตั้งใจ บันทึกไว้ใน `e2e/attempts.spec.ts`)
- เคยเห็น `e2e/proof-not-progress` แดงหนึ่งครั้งในการรันเต็มชุด แล้วเขียวสองรอบถัดมา
  โดยไม่แก้อะไร — น่าจะ flake แต่ยังไม่ได้ไล่หาสาเหตุ

Blocked on: งานถัดไปแตะ Pool A ซึ่งเป็นฐานข้อมูล production ที่ใช้ร่วมกับอีก 8 product และต้องใช้ credential ของเซิร์ฟเวอร์ · เจ้าของอนุมัติปากเปล่าในเซสชัน 2026-08-02 แล้ว แต่ handoff เป็นหลักฐาน ไม่ใช่ใบอนุญาต จึงส่งต่อการอนุมัตินั้นข้ามเซสชันไม่ได้

Required input: ยืนยันอีกครั้งว่าให้ดำเนินการบน Pool A ได้ (สร้างสคีมา academy + รัน migration + แก้ PGRST_DB_SCHEMAS + reload) และเลือกระหว่าง (ก) สร้าง Postgres role เฉพาะสคีมา academy แล้วออก JWT ของ role นั้น หรือ (ข) ใช้ service_role ร่วมไปก่อน · ถ้ายังไม่พร้อม ให้ข้ามไปทำงานในรายการ '✅ ทำได้เลย' แทน

## Exact Next Action
Working directory: academy-web

สร้างสคีมา academy บน Pool A + รัน migration 0001-0012 + เพิ่มเข้า PGRST_DB_SCHEMAS (เจ้าของอนุมัติในเซสชันก่อน ต้องยืนยันซ้ำ) แล้วต่อด้วย least-privilege role และ R2 + /media gating

First step: อ่าน ecosystem/SHARED_INFRA_ACCESS.md และ reports/state/supabase.md แล้วยืนยันกับเจ้าของว่ายังอนุมัติงานบน Pool A อยู่ จากนั้นตรวจสถานะจริงบนเซิร์ฟเวอร์ก่อนแตะอะไร (สคีมา academy มีอยู่แล้วหรือยัง PGRST_DB_SCHEMAS ปัจจุบันคืออะไร)

Commands:
- cat ../../../ecosystem/SHARED_INFRA_ACCESS.md
- sed -n '18,40p' ../../../reports/state/supabase.md
- ls academy-web/supabase/migrations/
- npx supabase db reset --local && npx vitest run   # ยืนยันว่า migration ทั้งชุดยังสะอาดในเครื่องก่อนขึ้น Pool A

## Done Definition
- สคีมา academy มีอยู่บน Pool A พร้อม migration 0001-0012 ครบ ตรวจได้จากรายชื่อตาราง/ฟังก์ชันจริง
- PGRST_DB_SCHEMAS มี academy และ PostgREST reload แล้ว (ยิง REST จริงได้)
- reports/state/supabase.md อัปเดตให้ตรงกับของจริงหลังทำ
- ยังไม่ตั้ง secret ของ Worker จนกว่าจะตัดสินเรื่อง least-privilege role

## Do Not Touch
- แก้ไฟล์ใน products/cyberskills/identity-control (งาน session อื่น อยู่ระหว่างทำ)
- push ไป remote ใดๆ (43+ commits ยังไม่ push โดยตั้งใจ)
- deploy Worker หรือสร้าง R2 bucket โดยไม่ได้ยืนยันกับเจ้าของในเซสชันใหม่
- รัน migration บน Pool A หรือแก้ PGRST_DB_SCHEMAS โดยไม่ได้ยืนยันสิทธิ์ในเซสชันใหม่
- commit pointer ของ academy ในรีโป director (guard ฟ้องไฟล์ของ identity-control อยู่)

รายละเอียด:
- `products/cyberskills/identity-control/**` — งานของ session อื่น อยู่ระหว่างทำ
  (เป็นรีโป git แยกที่ยังไม่ถูก track ในรีโป director) · ไฟล์สองไฟล์ในนี้ทำให้
  `scripts/check-shared-infra-docs.sh` ฟ้อง จึง commit อะไรในรีโป director ไม่ได้ —
  **แก้เองไม่ได้ ต้องให้เจ้าของงานนั้นเติมลิงก์ `SHARED_INFRA_ACCESS.md` เอง**
- การ push ไป remote ใดๆ (43+ commits ยังไม่ push โดยตั้งใจ)
- การ deploy Worker หรือสร้าง R2 bucket โดยไม่ยืนยันกับเจ้าของในเซสชันใหม่
- `artifacts/oneshot-2026-07-31/` และ `academy-web/artifacts/` — ภาพเหล่านี้ถูก e2e
  regen ทุกครั้งเพราะฝังอีเมลของบัญชีทดสอบ ให้ `git checkout --` คืนก่อน stage เสมอ

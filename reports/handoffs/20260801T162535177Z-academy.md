# Session Handoff: academy

<!-- session-handoff/v1
{
  "schema": "session-handoff/v1",
  "id": "20260801T162535177Z-academy",
  "created_at": "2026-08-01T16:25:35.177Z",
  "project": "academy",
  "objective": "ปิดช่องโหว่ความปลอดภัยสองข้อที่เหลือก่อนเปิดสมัครจริง แล้วทำใบรับรองให้ออกได้จริง",
  "state": "ready",
  "repo": {
    "remote": "github.com/teerakanok/academy-platform",
    "branch": "main",
    "base_head": "96d15d86ad5d1c7a546a66aa79b9262d52c7e334"
  },
  "delivery": "local",
  "worktree": {
    "mode": "clean",
    "entries": []
  },
  "scope": {
    "allowed": [
      "แก้โค้ดใน academy-web (โค้ด เทส เนื้อหา migration ที่รันบน local Supabase)",
      "รัน build / lint / vitest / playwright / wrangler dev ในเครื่อง",
      "commit ใน academy-platform เมื่อ verification เขียวครบ"
    ],
    "forbidden": [
      "แตะ Pool A (supabase.cyberskills.co.th) ทุกกรณี — มีอีก session ทำเรื่อง identity อยู่ สอง session แก้ GoTrue พร้อมกันจะทับกัน",
      "เปิด GOTRUE_DISABLE_SIGNUP / แก้ URI_ALLOW_LIST / แก้ PGRST_DB_SCHEMAS — เป็น decision ของ founder และของ session identity",
      "push โดยไม่ได้รับอนุญาต · deploy ขึ้น Cloudflare โดยไม่ได้รับอนุญาต",
      "แตะ working tree ของ director repo (มี workstream อื่นค้างอยู่ 41 ไฟล์)",
      "ชี้ app หรือเทสไปที่ prod DB — local Supabase เท่านั้น"
    ]
  },
  "canonical_read_order": [
    "AGENTS.md",
    "HOW_TO_PLAY.md",
    "plans/active_plan.md",
    "docs/adr/ADR-draft-single-account.md",
    "PENDING_USER_ACTION.md",
    "academy-web/src/lib/auth/enabled.ts"
  ],
  "owner_decisions": [
    "ต้องสมัครถ้าจะใช้ — บทเรียน/quiz/lab/progress ปิด · หน้าร้าน (landing, /courses, หน้าคอร์ส) เปิดสาธารณะ",
    "บัญชีเดียวใช้ได้ทุกบริการ CYBERSKILLS รวมถึง certification ในอนาคต — หน้าสมัครพูดในนาม CYBERSKILLS ไม่ใช่ Academy",
    "identity key = (issuer, subject) ห้ามใช้ email เป็น join key เด็ดขาด",
    "hosting เอียงไป Cloudflare (founder: จะย้ายงานอื่นไปด้วย ไม่อยาก maintain หลายที่)",
    "session identity อีกอันเป็นคนตัดสินเรื่อง single-account — Academy ปรับตามภายหลัง",
    "ช่องทางเข้าถึงลูกค้า: Google SEO · AI SEO · Facebook/X/LinkedIn",
    "แบรนด์เขียน CYBERSKILLS ตัวใหญ่ทั้งหมดในข้อความที่ผู้ใช้เห็น",
    "เว็บภาษาอังกฤษห้ามมีคำไทยหลุดแม้แต่คำเดียว"
  ],
  "completed": [
    "M3 แกนหลัก: auth ครบวงจร (รหัส 6 หลักทางอีเมล) + middleware gating + progress ผูกบัญชี",
    "CRITICAL แก้แล้ว: client เคยประกาศเองได้ว่าเรียนจบ → ปลอมใบรับรองได้ (พิสูจน์ 10/10 ก่อนแก้ · 0/10 หลังแก้)",
    "Simulation challenge: โจทย์จำลองหน้าจอตั้งค่า IPv4 ตัดสินจากสถานะสุดท้าย + สองโจทย์คำตอบตรงข้ามกัน",
    "i18n EN/TH: dictionary + สลับได้ + นโยบายความเป็นส่วนตัวสองภาษา + เทสกันอักษรไทยหลุดบนหน้าอังกฤษ",
    "วิดีโอหลายภาษา: สลับ source ตามภาษาเสียงโดยคืนตำแหน่ง + caption ผ่าน <track> + ย้าย quiz ออกจาก overlay มาไว้ใต้วิดีโอ",
    "deploy จริงขึ้น Cloudflare Workers (หน้าร้านอย่างเดียว บัญชีปิดโดยตั้งใจ)",
    "ADR single-account เคาะครบ 5 ข้อ + เปิด asymmetric JWT/JWKS บน Pool A (ปลด blocker STAR ที่ค้างตั้งแต่ 2026-06-13)",
    "copy: ตัดสำนวนที่อ่านออกว่าเครื่องเขียน 17 จุดในบทเรียน + 13 จุดใน UI"
  ],
  "changed_files": [
    {
      "path": "academy-web/src/app/api/progress/route.ts",
      "reason": "เซิร์ฟเวอร์ตรวจคำตอบเอง — client ส่งได้แค่ action/answers"
    },
    {
      "path": "academy-web/src/lib/auth/",
      "reason": "session, route client, accountsEnabled (มีคอมเมนต์กับดัก NEXT_PUBLIC)"
    },
    {
      "path": "academy-web/src/middleware.ts",
      "reason": "allowlist ของหน้าที่เปิดสาธารณะ + API ตอบ 401 ไม่ใช่ redirect"
    },
    {
      "path": "academy-web/src/lib/simulation/types.ts",
      "reason": "engine ตัดสินโจทย์จำลอง"
    },
    {
      "path": "academy-web/src/lib/i18n/",
      "reason": "dictionary EN/TH + นโยบายความเป็นส่วนตัวสองภาษา"
    },
    {
      "path": "academy-web/supabase/migrations/0002,0003",
      "reason": "ตาราง users/node_progress + ฟังก์ชัน atomic กันสถานะถอยหลัง"
    },
    {
      "path": "academy-web/e2e/progress-integrity.spec.ts",
      "reason": "ด่านกันย้อนกลับของช่องโหว่ CRITICAL"
    }
  ],
  "remaining_work": [
    "HIGH: rate limiter อยู่ใน memory ของ process และ key มาจาก header ที่ปลอมได้ → เดารหัส OTP 6 หลักได้ (บน Workers แต่ละ isolate มี memory แยก)",
    "HIGH: ไม่มี entitlement ต่อคอร์ส — middleware เช็คแค่ว่ามี user ไหม",
    "MEDIUM: ไฟล์บทเรียน (วิดีโอ/PDF) อยู่ใน public/ โหลดได้โดยไม่ล็อกอิน",
    "MEDIUM: shouldCreateUser:true บน endpoint สาธารณะ — ยิงอีเมลจำนวนมากได้",
    "MEDIUM: MAX_TRACKED_KEYS ไม่ใช่เพดานจริง",
    "ใบรับรอง: มีแค่กติกาว่าใครมีสิทธิ์ ยังขาดหน้ากรอกชื่อจริง + ตัวเอกสาร + หน้า verify",
    "โหมด assessed ของ simulation ยังไม่ผูกเข้า checkpoint",
    "ภาพประกอบบทเรียนทำแล้ว 3 จาก 10 บท",
    "i18n เป็น client-side — ควรแยก route ต่อภาษา (/th/...) เพื่อ SEO",
    "src/lib/content/source.ts (engine ของ /player) ยังอ่านดิสก์ → พังบน Workers"
  ],
  "risks": [
    "อีก session ทำเรื่อง identity อยู่ — ถ้าเขาเคาะให้มี central sign-in หน้า /sign-in ของ Academy ต้องกลายเป็น redirect",
    "prod ยังไม่มี schema academy และ GOTRUE_DISABLE_SIGNUP ยัง true — เปิดสมัครจริงไม่ได้จนกว่าจะเคาะ",
    "ถ้าแก้ rate limit ผิดทาง (ยังอยู่ใน memory) จะดูเหมือนแก้แล้วแต่ยังเดา OTP ได้เหมือนเดิม"
  ],
  "next": {
    "cwd": "academy-web",
    "summary": "แทนที่ rate limiter ใน memory ด้วย limiter แบบ distributed เพื่อกันการเดารหัส OTP",
    "first_step": "อ่าน academy-web/src/lib/rate-limit.ts และ src/app/api/auth/verify/route.ts แล้วออกแบบ limiter ที่ไม่พึ่ง memory ของ process (KV หรือ Durable Object ของ Cloudflare หรือตาราง Postgres) โดยจำกัดทั้งต่อ IP และต่ออีเมล และล็อก challenge หลังตอบผิดเกินจำนวนที่กำหนด",
    "commands": [
      "cd products/cyberskills/academy-platform/academy-web",
      "./node_modules/.bin/supabase start   # ถ้ายังไม่ได้รัน",
      "npm run build && npm run lint && npm run test && npm run test:e2e"
    ],
    "acceptance": [
      "ยิง POST /api/auth/verify ด้วยรหัสผิดซ้ำๆ โดยเปลี่ยน header cf-connecting-ip ทุกครั้ง แล้วต้องยังโดนจำกัด (ไม่ใช่ได้โควตาใหม่)",
      "มีเทสอัตโนมัติที่พิสูจน์ข้อข้างบน ไม่ใช่ทดสอบด้วยมืออย่างเดียว",
      "full chain เขียว: build + lint + vitest + playwright",
      "ผู้เรียนที่ใส่รหัสถูกครั้งแรกยังล็อกอินได้ตามปกติ"
    ],
    "execution_boundary": "local-reversible"
  },
  "blocker": null,
  "verification": [
    {
      "command": "npm run build && npm run lint && npm run test && npm run test:e2e",
      "result": "ผ่านทั้งหมด — vitest 139/139 · playwright 50/50"
    },
    {
      "command": "bash exploit-check (ยิง POST /api/progress ประกาศ completed 10 บท)",
      "result": "0/10 หลังแก้ (ก่อนแก้ 10/10) และไม่มีสิทธิ์ใบรับรอง"
    },
    {
      "command": "curl production /sign-in",
      "result": "ขึ้นป้าย \"ยังไม่เปิด\" ไม่มีฟอร์มที่กดแล้วพัง"
    },
    {
      "command": "node session-cleanup.mjs verify",
      "result": "clean=true · 0 paths · 0 processes · exit 0"
    }
  ],
  "cleanup": {
    "processes": "หยุด wrangler dev และ next-server แล้ว · local Supabase (12 container) ยังรันอยู่โดยตั้งใจ — เป็นของยืนพื้นสำหรับพัฒนา ไม่ใช่ residue ของ session นี้ (เริ่มใหม่ด้วย ./node_modules/.bin/supabase start ถ้าถูกปิดไป)",
    "artifacts": "session-cleanup/v1 clean=true; session residue: 0 paths (0 B reclaimable), 0 processes — scratchpad 27.8 MB ถูกกวาดแล้ว · ภาพหลักฐานที่ยังต้องเก็บอยู่ใน artifacts/ และ commit แล้ว"
  }
}
-->

## Objective
ปิดช่องโหว่ความปลอดภัยสองข้อที่เหลือก่อนเปิดสมัครจริง แล้วทำใบรับรองให้ออกได้จริง

## Owner Intent And Decisions
- Decision: ต้องสมัครถ้าจะใช้ — บทเรียน/quiz/lab/progress ปิด · หน้าร้าน (landing, /courses, หน้าคอร์ส) เปิดสาธารณะ
- Decision: บัญชีเดียวใช้ได้ทุกบริการ CYBERSKILLS รวมถึง certification ในอนาคต — หน้าสมัครพูดในนาม CYBERSKILLS ไม่ใช่ Academy
- Decision: identity key = (issuer, subject) ห้ามใช้ email เป็น join key เด็ดขาด
- Decision: hosting เอียงไป Cloudflare (founder: จะย้ายงานอื่นไปด้วย ไม่อยาก maintain หลายที่)
- Decision: session identity อีกอันเป็นคนตัดสินเรื่อง single-account — Academy ปรับตามภายหลัง
- Decision: ช่องทางเข้าถึงลูกค้า: Google SEO · AI SEO · Facebook/X/LinkedIn
- Decision: แบรนด์เขียน CYBERSKILLS ตัวใหญ่ทั้งหมดในข้อความที่ผู้ใช้เห็น
- Decision: เว็บภาษาอังกฤษห้ามมีคำไทยหลุดแม้แต่คำเดียว

Allowed scope:
- แก้โค้ดใน academy-web (โค้ด เทส เนื้อหา migration ที่รันบน local Supabase)
- รัน build / lint / vitest / playwright / wrangler dev ในเครื่อง
- commit ใน academy-platform เมื่อ verification เขียวครบ

## Repository State
- State: ready
- Branch: main
- Baseline: 96d15d86ad5d1c7a546a66aa79b9262d52c7e334
- Delivery: local

## Completed This Session
- M3 แกนหลัก: auth ครบวงจร (รหัส 6 หลักทางอีเมล) + middleware gating + progress ผูกบัญชี
- CRITICAL แก้แล้ว: client เคยประกาศเองได้ว่าเรียนจบ → ปลอมใบรับรองได้ (พิสูจน์ 10/10 ก่อนแก้ · 0/10 หลังแก้)
- Simulation challenge: โจทย์จำลองหน้าจอตั้งค่า IPv4 ตัดสินจากสถานะสุดท้าย + สองโจทย์คำตอบตรงข้ามกัน
- i18n EN/TH: dictionary + สลับได้ + นโยบายความเป็นส่วนตัวสองภาษา + เทสกันอักษรไทยหลุดบนหน้าอังกฤษ
- วิดีโอหลายภาษา: สลับ source ตามภาษาเสียงโดยคืนตำแหน่ง + caption ผ่าน <track> + ย้าย quiz ออกจาก overlay มาไว้ใต้วิดีโอ
- deploy จริงขึ้น Cloudflare Workers (หน้าร้านอย่างเดียว บัญชีปิดโดยตั้งใจ)
- ADR single-account เคาะครบ 5 ข้อ + เปิด asymmetric JWT/JWKS บน Pool A (ปลด blocker STAR ที่ค้างตั้งแต่ 2026-06-13)
- copy: ตัดสำนวนที่อ่านออกว่าเครื่องเขียน 17 จุดในบทเรียน + 13 จุดใน UI

## Changed Files
- `academy-web/src/app/api/progress/route.ts`: เซิร์ฟเวอร์ตรวจคำตอบเอง — client ส่งได้แค่ action/answers
- `academy-web/src/lib/auth/`: session, route client, accountsEnabled (มีคอมเมนต์กับดัก NEXT_PUBLIC)
- `academy-web/src/middleware.ts`: allowlist ของหน้าที่เปิดสาธารณะ + API ตอบ 401 ไม่ใช่ redirect
- `academy-web/src/lib/simulation/types.ts`: engine ตัดสินโจทย์จำลอง
- `academy-web/src/lib/i18n/`: dictionary EN/TH + นโยบายความเป็นส่วนตัวสองภาษา
- `academy-web/supabase/migrations/0002,0003`: ตาราง users/node_progress + ฟังก์ชัน atomic กันสถานะถอยหลัง
- `academy-web/e2e/progress-integrity.spec.ts`: ด่านกันย้อนกลับของช่องโหว่ CRITICAL

## Verification
- `npm run build && npm run lint && npm run test && npm run test:e2e` → ผ่านทั้งหมด — vitest 139/139 · playwright 50/50
- `bash exploit-check (ยิง POST /api/progress ประกาศ completed 10 บท)` → 0/10 หลังแก้ (ก่อนแก้ 10/10) และไม่มีสิทธิ์ใบรับรอง
- `curl production /sign-in` → ขึ้นป้าย "ยังไม่เปิด" ไม่มีฟอร์มที่กดแล้วพัง
- `node session-cleanup.mjs verify` → clean=true · 0 paths · 0 processes · exit 0

## Dirty State
Expected worktree: clean (`git status --short` = 0 รายการ ณ เวลาปิด)

งานทั้งหมดของ session นี้ commit แล้วและ push ขึ้น origin/main แล้ว ไม่มีไฟล์ที่ตั้งใจ
ทิ้งไว้ให้ทำต่อแบบ dirty

## Cleanup State
- Processes: หยุด wrangler dev และ next-server แล้ว · local Supabase (12 container) ยังรันอยู่โดยตั้งใจ — เป็นของยืนพื้นสำหรับพัฒนา ไม่ใช่ residue ของ session นี้ (เริ่มใหม่ด้วย ./node_modules/.bin/supabase start ถ้าถูกปิดไป)
- Artifacts: session-cleanup/v1 clean=true; session residue: 0 paths (0 B reclaimable), 0 processes — scratchpad 27.8 MB ถูกกวาดแล้ว · ภาพหลักฐานที่ยังต้องเก็บอยู่ใน artifacts/ และ commit แล้ว

## Remaining Work And Risks
Remaining:
- HIGH: rate limiter อยู่ใน memory ของ process และ key มาจาก header ที่ปลอมได้ → เดารหัส OTP 6 หลักได้ (บน Workers แต่ละ isolate มี memory แยก)
- HIGH: ไม่มี entitlement ต่อคอร์ส — middleware เช็คแค่ว่ามี user ไหม
- MEDIUM: ไฟล์บทเรียน (วิดีโอ/PDF) อยู่ใน public/ โหลดได้โดยไม่ล็อกอิน
- MEDIUM: shouldCreateUser:true บน endpoint สาธารณะ — ยิงอีเมลจำนวนมากได้
- MEDIUM: MAX_TRACKED_KEYS ไม่ใช่เพดานจริง
- ใบรับรอง: มีแค่กติกาว่าใครมีสิทธิ์ ยังขาดหน้ากรอกชื่อจริง + ตัวเอกสาร + หน้า verify
- โหมด assessed ของ simulation ยังไม่ผูกเข้า checkpoint
- ภาพประกอบบทเรียนทำแล้ว 3 จาก 10 บท
- i18n เป็น client-side — ควรแยก route ต่อภาษา (/th/...) เพื่อ SEO
- src/lib/content/source.ts (engine ของ /player) ยังอ่านดิสก์ → พังบน Workers

Risks:
- อีก session ทำเรื่อง identity อยู่ — ถ้าเขาเคาะให้มี central sign-in หน้า /sign-in ของ Academy ต้องกลายเป็น redirect
- prod ยังไม่มี schema academy และ GOTRUE_DISABLE_SIGNUP ยัง true — เปิดสมัครจริงไม่ได้จนกว่าจะเคาะ
- ถ้าแก้ rate limit ผิดทาง (ยังอยู่ใน memory) จะดูเหมือนแก้แล้วแต่ยังเดา OTP ได้เหมือนเดิม

No blocker.

## Exact Next Action
Working directory: academy-web

แทนที่ rate limiter ใน memory ด้วย limiter แบบ distributed เพื่อกันการเดารหัส OTP

First step: อ่าน academy-web/src/lib/rate-limit.ts และ src/app/api/auth/verify/route.ts แล้วออกแบบ limiter ที่ไม่พึ่ง memory ของ process (KV หรือ Durable Object ของ Cloudflare หรือตาราง Postgres) โดยจำกัดทั้งต่อ IP และต่ออีเมล และล็อก challenge หลังตอบผิดเกินจำนวนที่กำหนด

Commands:
- `cd products/cyberskills/academy-platform/academy-web`
- `./node_modules/.bin/supabase start   # ถ้ายังไม่ได้รัน`
- `npm run build && npm run lint && npm run test && npm run test:e2e`

## Done Definition
- ยิง POST /api/auth/verify ด้วยรหัสผิดซ้ำๆ โดยเปลี่ยน header cf-connecting-ip ทุกครั้ง แล้วต้องยังโดนจำกัด (ไม่ใช่ได้โควตาใหม่)
- มีเทสอัตโนมัติที่พิสูจน์ข้อข้างบน ไม่ใช่ทดสอบด้วยมืออย่างเดียว
- full chain เขียว: build + lint + vitest + playwright
- ผู้เรียนที่ใส่รหัสถูกครั้งแรกยังล็อกอินได้ตามปกติ
## Do Not Touch
- แตะ Pool A (supabase.cyberskills.co.th) ทุกกรณี — มีอีก session ทำเรื่อง identity อยู่ สอง session แก้ GoTrue พร้อมกันจะทับกัน
- เปิด GOTRUE_DISABLE_SIGNUP / แก้ URI_ALLOW_LIST / แก้ PGRST_DB_SCHEMAS — เป็น decision ของ founder และของ session identity
- push โดยไม่ได้รับอนุญาต · deploy ขึ้น Cloudflare โดยไม่ได้รับอนุญาต
- แตะ working tree ของ director repo (มี workstream อื่นค้างอยู่ 41 ไฟล์)
- ชี้ app หรือเทสไปที่ prod DB — local Supabase เท่านั้น

**เหตุผลของข้อแรก:** founder ระบุว่ามีอีก session ทำเรื่อง identity control
(บัญชีเดียวใช้ได้ทุก product) อยู่ และ **session นั้นเป็นคนตัดสิน Academy ปรับตาม
ภายหลัง** — สอง session แก้ GoTrue/PGRST ของ Pool A พร้อมกันจะทับกันแน่นอน

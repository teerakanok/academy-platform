R1 Worker database environment finding confirmed by two executable regressions: missing process.env with explicit Worker bindings previously fails, and missing supplied bindings previously silently uses ambient credentials. Both now corrected by passing the supplied environment through the default database factory, preserving no-argument Next behavior. Full unit2430, app/worker/retention TypeScript, real-font build plus original workerd gate and actual final module startup all exit0. Lint log hash matches approved3-errors16-warnings baseline exactly. Source approval only, no new Worker deployment.

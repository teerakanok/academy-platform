# Controller acceptance — Academy cookie isolation

Author implementation was independently inspected by the director at the security/customer-critical boundary. Production readers accept only the browser-enforced `__Host-academy_session`; session and transaction-binding issuance uses Secure, Path=/ and no Domain. Legacy production cookies do not become fallback authentication. Duplicate/malformed host cookies are rejected. HTTP local fixtures retain an explicit legacy path. Media, callback retry binding, and sign-out paths were inspected together.

Root causal verification temporarily restored the legacy host constant: `NODE_ENV=production npx vitest run --project unit tests/unit/host-session-cookie-isolation.test.ts` exited1; exact source restoration exited0. Full `npx vitest run --project unit`: 2445 passed, 2 skipped. App, Worker and retention TypeScript commands exited0; see root gate receipt. `npm run lint` exited1 with only the accepted three no-require-imports errors in academy-bound-worker-executor.cjs and16warnings.

Actual `npm run build:cf` exposed five stale cookie literals in the workerd fixture; these were migrated while retaining its denied-session assertions. The next build passed all13 real-workerd checks and compiled OpenNext, then exposed an existing verifier launching the native workerd binary through Node. Controller changed only that invocation to the installed .bin/workerd executable. Focused final-startup verification then exited0, and complete final `npm run build:cf` exited0 at2026-09-07T17:22:03.867867Z, including13workerdchecks,83cacheassets,assetguard and final raw-host404 startup gate. Node25.5.0 was used; repository declares24.x.

Raw logs are retained byte-for-byte and may contain trailing whitespace; evidence files are excluded only from whitespace-only review. Root corrections affect test fixtures and the build verifier, not authored authentication runtime. This is source/build acceptance, not deployed cookie or real learner journey acceptance. Cutover requires fresh sign-in for legacy sessions/in-flight callbacks; rollback likewise requires fresh sign-in. No database or live deployment action is claimed here.

# Operator installer acceptance

Upgrade preserves existing configuration and foreign entries while replacing only the exact managed 9/10-file contract. New preflight refuses a foreign file occupying the newly introduced tenth path before mutation. Per-file journaling supports partial moves; legacy whole-directory journals restore the prior directory without discarding retained configuration. Cleanup requires names, hashes, file metadata and directory custody to match. Unexpected COMMITTED residue remains preserved and blocks automatic removal.

Independent reviewer read the full installer and found rollback could unlink healthy new files before validating a corrupt prior managed backup. Author reproduced this; fix prevalidates the complete rollback source and affected live/stage state. Root causally removed that validation: focused test exit1 from changed live snapshot on rejected NEW_LIVE recovery. Exact source restored, focused test exit0. Snapshot checks cover path, inode/device, ownership, permissions, size and hash. This is a concrete data-preservation regression, not a claim about every filesystem failure.

Root required gates: node --test scripts/academy-production-operation.test.mjs exit0 (one file containing integrated filesystem assertions); npx vitest run --project unit exit0,150 files/2441 pass/2skip; npm run lint exit1 with exactly the accepted three no-require-imports errors in academy-bound-worker-executor.cjs and17warnings. Lint short-circuits before its TypeScript commands; no typecheck-success claim from that command. No app/runtime TypeScript changed.

Local accepted only. No live installer execution or entitlement mutation. sudo -n id -u currently fails because a password is required; native UI tooling unavailable. Installation must revalidate actual predecessor file digests/metadata and preserve root-owned p1-p7-config.json. Before live entitlement canary, database lock/statement timeouts and mandatory ROLLBACK rehearsal remain a separate work item. Do not replay historical identity/schema activation migrations.

# Academy operator rehearsal — source accepted, execution pending

Source `0de696a`, branch `fix/operator-rehearsal-cde63a58`. No application Worker deployment is required for these operator CLI additions. Migration0036 and real owner/staff/entitlement grants are NOT applied to production by this report.

Existing dry-run modes only inspected state. New explicit --rehearse modes perform the same authorized RPC inside a transaction, verify intended state/latest audit, ROLLBACK, and verify restoration. Default inspection and explicit apply remain. Two dedicated-role read-only audit projections support latest-event identity verification without table SELECT or runtime grants.

GLM5.3-flash/max authored retained patch676edb94c3a3720724772269136baf44a583824511b2a65f027be93f77a65f7a, work order wo-5000854a-c0b8-4123-9713-27516a99a670, provider run run-48262fc71e8c6470e4f1c4942b50d5fb. Author advisory was schema-valid; dispatcher chain still recorded retained_patch/quarantined_advisory with stop_reason harness_or_patch_unavailable. Root does not relabel that sealed result or treat the author summary as a gate. Token usage unknown; actual author output was useful and accepted with bounded correction.

Root original CLI causal check proved --rehearse silently acted as inspection. Root real PostgreSQL then found candidate SQLSTATE08P01 (three placeholders/two parameters in audit callers) and a first-owner verifier that incorrectly required actorAuthorized to remain false. The machine-routed small controller correction fixed both call sites and allowed true only for self owner enable; the audited write RPC remains authoritative. Strict bind-arity and first-owner regressions went from5failed/1passed to6passed. Full unit2466passed/2skipped across155files; lint retains exactly3baseline errors/17warnings.

Actual dedicated-role PostgreSQL acceptance in the owned disposable clone passed6cases: first-owner and entitlement rehearsal/apply plus existing-grant no-op rehearsal. Every rehearsal restored all compared staff/audit/entitlement row digests. Unauthorized actors produced42501 and unchanged rows in2negative cases. Sequences may advance despite rollback; no sequence-restoration claim. Independent9file review PASS binds final bytes.

Production inspection found no active Academy owner and no passwords configured on either dedicated operator role. Unix socket direct login is peer-rejected. No password, role, authentication policy or production account state was changed. Honor the owner's0600-file custody requirement for new operator inputs; do not replace the dedicated-login boundary with a shared/application role. Before0036 use, bind absence/ownership of both new functions, rehearse the exact migration with ROLLBACK, then COMMIT and independently verify ACL/body. The production Worker remains3830694b@100/sourcefe4220a; reported sign-in loop already closed separately.

Persistent learning: operator tests must exercise the actual SQL parameter contract and the first-owner transition, not only an already-authorized fake. The shared fake now rejects mismatched placeholder arity, and the first-owner regression records authorization changing inside the transaction. Root real-PostgreSQL row-digest proof remains the acceptance oracle for mutation rollback; a successful inspect-only command is not rehearsal evidence.

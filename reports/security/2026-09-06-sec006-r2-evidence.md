# SEC006 r2 local evidence — 2026-09-06

## Outcome

Continued the retained SEC006 patch without rewriting its application surface. The r2 correction makes assessed retry ordering use `result_recorded_at desc, attempt_id desc`, starts both first-failure (5 minutes) and repeat-failure (15 minutes) cooldown from completion time, pins SQL function search paths, and explicitly resolves the integrity helper through `academy`. The integration fixture now covers dwell, first/repeat cooldown, completion ordering, caller search-path resolution, concurrent admission, production rolling clamp, and the UTC daily ceiling without weakening assertions.

Historical SEC006 artifacts remain unchanged. The r2 logs below are unique evidence files.

## Evidence

| Gate | Exact command (from repository root unless noted) | Exit | SHA-256 |
|---|---|---:|---|
| r2 RED unit | `cd academy-web && npx vitest run --project unit assessment-attempt-integrity --maxWorkers 1` | 1 | `1fa8c91886b45742861bcbbbdabdcc49910ffe4b7516c7ceedd4f7ce5b55771e` |
| r2 RED integration | `cd academy-web && TEST_DATABASE_URL='postgresql://fixture_owner@localhost:65433/postgres?host=/private/tmp/postgres-fixture-cde63a58/socket' npx vitest run --project integration assessment-attempt-integrity --maxWorkers 1` | 1 | `07d7049df517a14f43e5cef6ac535c02bdce9d961ec1f957f216ce4e092fcf73` |
| r2 focused GREEN | `cd academy-web && npx vitest run --project unit assessment-attempt-integrity attempt --maxWorkers 1` | 0 | `7e21fa136f6507d027da25523b650b9968cc0d77491c2437a5983dd5f0370bd1` |
| r2 full unit | `cd academy-web && npx vitest run --project unit --maxWorkers 1` | 0 | `f2934af88bb924f9f5b5a3860a9f8891ceda8829e541c72db040ae9999db4119` |
| r2 TypeScript | `cd academy-web && npx tsc --noEmit && npx tsc -p tsconfig.worker.json` | 0 | `418a5c17f33c70e99b0cc0a07fce69191489cfedc94164bfa903785777c5bd4b` |
| r2 lint baseline | `cd academy-web && npm run lint` | 1 (existing 3 errors / 16 warnings) | `0320dbd183ad71d66ea873579242500a95ac223a78a1bfb039da5904aeae5cd9` |
| required gate | `cd academy-web && npm ci --ignore-scripts --no-audit && npx vitest run --project unit attempt capstone progress --maxWorkers 1 && TEST_DATABASE_URL='postgresql://fixture_owner@localhost:65433/postgres?host=/private/tmp/postgres-fixture-cde63a58/socket' npx vitest run --project integration assessment-attempt-integrity --maxWorkers 1 && npx tsc --noEmit && npx tsc -p tsconfig.worker.json` | 1 | `a94eab8173436ec1283dbbe54fead2d9ff80ba2a60fb51f2116d2da82085cd97` |

The required gate’s unit segment passed `10/10` files and `161/161` tests. Full unit passed `146/146` files and `2,417/2,417` tests. App and worker TypeScript passed when run separately after the gate.

## Apparatus blocker

The controller-provided PostgreSQL socket file exists at the exact permitted path, but both integration tests fail before migration execution with `connect EPERM /private/tmp/postgres-fixture-cde63a58/socket/.s.PGSQL.65433`. This is a local execution-apparatus permission failure, not a SQL assertion failure. No TCP fallback, alternate database, cluster control, or cluster file was used.

Therefore the durable SQL behavior is retained and covered by the strengthened fixture, but this run cannot claim real-PostgreSQL GREEN or outcome closure. The existing lint baseline remains exactly three pre-existing errors and sixteen warnings.

## Remaining gates

- Re-run the exact integration gate in an execution context allowed to connect to the provided Unix socket; all fixtures roll back.
- Crucible must expand every certificate-bearing capstone bank to at least 15 authored MCQs before full SEC006 content closure.
- No production migration, deployment, external service operation, message, or production closure is claimed.

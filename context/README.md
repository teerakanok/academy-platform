# Academy Context

Durable Academy context lives here when it does not belong in `plans/`,
`reports/`, `artifacts/`, or `docs/`.

Keep source-of-truth content portable and link to upstream owners such as
Crucible rather than duplicating large source assets.

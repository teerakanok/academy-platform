{
  "nodeId": "recursion-in-assembly",
  "locale": "en",
  "title": "Capstone: measuring what a recursive call costs",
  "objective": "By the end of this capstone you can explain recursion entirely in terms of frames, predict a program's maximum recursion depth from two measurements, and recognise when the compiler has removed the recursion altogether.",
  "blocks": [
    {
      "kind": "paragraph",
      "text": "Recursion is often taught as a concept requiring a leap of faith. In assembly there is no leap: it is a function that calls a function, and the only thing that makes it work is that each call gets its own frame. This capstone makes that concrete and then measures it."
    },
    {
      "kind": "heading",
      "text": "The whole mechanism"
    },
    {
      "kind": "code",
      "caption": "Factorial, compiled at -O0 so nothing is hidden",
      "lines": [
        "  long fact(long n) { return n <= 1 ? 1 : n * fact(n-1); }",
        "",
        "  _fact:                              compiled at -O0",
        "      push  rbp",
        "      mov   rbp, rsp",
        "      sub   rsp, 32                   ; a frame, every single call",
        "      mov   qword ptr [rbp - 8], rdi  ; n",
        "      cmp   qword ptr [rbp - 8], 1",
        "      jg    .Lrecurse",
        "      mov   eax, 1",
        "      mov   qword ptr [rbp - 16], rax  ## 8-byte Spill",
        "      jmp   .Ldone",
        "  .Lrecurse:",
        "      mov   rax, qword ptr [rbp - 8]",
        "      mov   qword ptr [rbp - 24], rax  ## 8-byte Spill   <- n saved across the call",
        "      mov   rdi, qword ptr [rbp - 8]",
        "      sub   rdi, 1",
        "      call  _fact                      ; <- the recursive call",
        "      mov   rcx, rax",
        "      mov   rax, qword ptr [rbp - 24]  ## 8-byte Reload   <- n recovered",
        "      imul  rax, rcx",
        "      mov   qword ptr [rbp - 16], rax  ## 8-byte Spill",
        "  .Ldone:",
        "      mov   rax, qword ptr [rbp - 16]  ## 8-byte Reload",
        "      add   rsp, 32",
        "      pop   rbp",
        "      ret",
        "",
        "/* Nothing about recursion is special. It is one function calling a",
        "   function, which happens to be itself. What makes it work is that each",
        "   call gets its own frame, so each call's n is a different 8 bytes of",
        "   memory. The spill of n before the call and its reload afterwards is",
        "   the whole mechanism, stated in two instructions. */"
      ]
    },
    {
      "kind": "callout",
      "tone": "info",
      "title": "The spill before the call is the entire secret",
      "text": "The function needs n after the recursive call returns, in order to multiply. But the call may destroy any caller-saved register, and rdi in particular will hold a different value inside the callee. So n is written to this frame's own memory before the call and read back afterwards. Every recursive algorithm you have written works because of that store and load — the values of the outer call sit in memory that the inner call cannot reach, because the inner call's frame is somewhere else entirely."
    },
    {
      "kind": "heading",
      "text": "Experiment one: how deep can it go"
    },
    {
      "kind": "paragraph",
      "text": "Recursion consumes stack. That statement is usually left there. It can be made exact with two independent measurements and one division."
    },
    {
      "kind": "code",
      "caption": "Measured on macOS with a default 8 MB main-thread stack",
      "lines": [
        "  Three measurements, taken separately, that predict each other:",
        "",
        "  1. Frame size, measured by printing addresses of locals at each depth:",
        "         frame 1: this frame is 32 bytes below the previous",
        "         frame 2: this frame is 32 bytes below the previous",
        "         frame 3: this frame is 32 bytes below the previous",
        "",
        "  2. Stack limit, from the shell:",
        "         $ ulimit -s",
        "         8176            (kilobytes)",
        "",
        "  3. Prediction:  8176 x 1024 / 32  =  261,632 frames",
        "",
        "  4. Measured, by catching SIGSEGV on an alternate stack and printing",
        "     the depth reached:",
        "         stack exhausted after 261,448 frames",
        "",
        "/* 261,448 against a predicted 261,632 -- 0.07% apart. The shortfall is the",
        "   frames already on the stack before the recursion started. Three separate",
        "   measurements agreeing to within a fraction of a percent is what turns",
        "   'recursion uses stack' from a slogan into an engineering quantity. */"
      ]
    },
    {
      "kind": "callout",
      "tone": "tip",
      "title": "This is a number you can use",
      "text": "Once you can measure a frame size and read a stack limit, you can answer a real engineering question: is this recursive algorithm safe on my inputs? A tree walk over a balanced structure of a million nodes goes twenty levels deep and is fine. The same walk over a degenerate structure goes a million levels deep and dies at about a quarter of that. The difference between those two cases is not a matter of style, and now it is a calculation rather than a hope."
    },
    {
      "kind": "heading",
      "text": "Experiment two: when the recursion is not there"
    },
    {
      "kind": "code",
      "caption": "The same functions at -O2",
      "lines": [
        "  long fact_tail(long n, long acc) { return n <= 1 ? acc : fact_tail(n-1, n*acc); }",
        "",
        "  At -O2 there is NO call instruction in this function at all:",
        "",
        "  .Lloop:",
        "      imul rax, rdi         ; acc *= n",
        "      dec  rdi              ; n--",
        "      dec  rdx",
        "      jne  .Lloop           ; a backward jump: this is a LOOP",
        "",
        "  And the non-tail version, fact, was ALSO turned into a loop at -O2 --",
        "  and unrolled eight times on top of that.",
        "",
        "/* Tail-call elimination is the rule that a call in tail position needs no",
        "   new frame, because the current one has no further use for itself. Here",
        "   the compiler went further and eliminated the call entirely, in both the",
        "   tail-recursive and the ordinary version. Recursion depth in the source",
        "   says nothing about stack depth in the binary. */"
      ]
    },
    {
      "kind": "paragraph",
      "text": "This is the result that matters most for reading real binaries. A recursive function in the source may be an ordinary loop in the binary, with no call, no frame growth and no depth limit at all. Conversely, a function that looks iterative may recurse through a function pointer the disassembler cannot follow. The source tells you the author's intent; only the binary tells you what will run."
    },
    {
      "kind": "heading",
      "text": "When elimination is possible, and when it is not"
    },
    {
      "kind": "table",
      "headers": [
        "Shape",
        "Can the call be eliminated",
        "Why"
      ],
      "rows": [
        [
          "return f(x)",
          "Often, when the ABI permits",
          "Nothing remains after the call, but ABI, type, variadic, stack-cleanup and compiler constraints still decide whether it can become a jump"
        ],
        [
          "return n * f(x)",
          "Only by a stronger transformation",
          "Work remains after the call; here the compiler rewrote it as a loop anyway"
        ],
        [
          "Two recursive calls, as in a tree walk",
          "One of them at best",
          "Only the last call can be in tail position"
        ],
        [
          "A call through a function pointer",
          "Yes",
          "It becomes an indirect jmp; an unknown target blocks inlining, not elimination"
        ],
        [
          "Any call in a function that took a local's address",
          "Usually no",
          "The frame may still be referenced after the call"
        ]
      ]
    },
    {
      "kind": "paragraph",
      "text": "The last row explains a common surprise: adding a debugging print or taking the address of a local can prevent tail-call elimination and turn a program that ran fine into one that overflows the stack. If a recursive program works at one optimisation level and crashes at another, this is where to look first."
    },
    {
      "kind": "heading",
      "text": "Your task"
    },
    {
      "kind": "list",
      "items": [
        "Measure your own machine's per-frame cost and stack limit, and predict a maximum depth",
        "Verify the prediction by catching the fault and reporting the depth reached",
        "Compile a recursive function at -O0 and -O2 and determine whether the call survives",
        "Rewrite it in tail-recursive form and check whether that changes the answer",
        "Explain each result in terms of frames rather than in terms of recursion"
      ]
    },
    {
      "kind": "code",
      "caption": "How to catch the overflow rather than just crashing",
      "lines": [
        "  #define _XOPEN_SOURCE 700",
        "  #include <limits.h>",
        "  #include <signal.h>",
        "  #include <string.h>",
        "  #include <unistd.h>",
        "",
        "  static char altstack[64 * 1024];   /* not SIGSTKSZ: on glibc >= 2.34 it is a sysconf call, not a constant */",
        "  static volatile sig_atomic_t depth = 0;",
        "  static sig_atomic_t limit = 1000;  /* normal run is bounded */",
        "",
        "  __attribute__((noinline)) static void recurse(void) {",
        "      volatile unsigned char frame[64];",
        "      frame[0] = (unsigned char)depth;",
        "      depth++;",
        "      if (depth < limit) recurse();",
        "      if (frame[0] == 255 && depth == 0) _exit(3); /* keep frame live */",
        "  }",
        "",
        "  static void onsegv(int sig) {",
        "      /* the normal stack is gone, so this handler runs on its own.",
        "         snprintf is NOT on the async-signal-safe list, so format by hand. */",
        "      char buf[32];",
        "      int n = sizeof buf;",
        "      unsigned long v = (unsigned long)depth;",
        "      buf[--n] = '\\n';",
        "      do { buf[--n] = '0' + (char)(v % 10); v /= 10; } while (v && n);",
        "      write(2, buf + n, sizeof buf - n);   /* write IS on the safe list */",
        "      _exit(128 + sig);",
        "  }",
        "",
        "  static int install_handler(void) {",
        "      stack_t ss = { .ss_sp = altstack, .ss_size = sizeof altstack };",
        "      if (sigaltstack(&ss, NULL) == -1) return -1;",
        "      struct sigaction sa = {0};",
        "      sa.sa_handler = onsegv;",
        "      sa.sa_flags   = SA_ONSTACK;  /* <- without this the handler cannot run */",
        "      sigemptyset(&sa.sa_mask);",
        "      if (sigaction(SIGSEGV, &sa, NULL) == -1) return -1;",
        "      return 0;",
        "  }",
        "",
        "  int main(int argc, char **argv) {",
        "      if (argc == 2 && strcmp(argv[1], \"--exhaust\") == 0) limit = INT_MAX;",
        "      if (install_handler() == -1) _exit(1);",
        "      recurse();",
        "      return depth == limit ? 0 : 2;",
        "  }",
        "",
        "/* SA_ONSTACK and sigaltstack are what make this work: the handler needs",
        "   stack, and the ordinary stack is exactly what has just run out. The",
        "   async-signal-safety rule from the operating systems course applies in",
        "   full -- write is on the safe list, snprintf is not, so the digits are",
        "   produced by hand. Compile with cc -O0 -o stack-depth stack-depth.c.",
        "   ./stack-depth is a bounded verification run. Run --exhaust only in a",
        "   resource-limited child process, for example:",
        "   (ulimit -s 256; timeout 3 ./stack-depth --exhaust) */"
      ]
    },
    {
      "kind": "callout",
      "tone": "warning",
      "title": "Compile with -fno-optimize-sibling-calls for the depth experiment",
      "text": "Without it the compiler may eliminate the very calls you are trying to count, and the program will loop forever instead of overflowing. That is not the experiment failing — it is the second experiment succeeding by accident. Being able to tell those two outcomes apart is the point of doing both."
    },
    {
      "kind": "heading",
      "text": "What you should be able to say at the end"
    },
    {
      "kind": "table",
      "headers": [
        "Question",
        "The answer your own numbers support"
      ],
      "rows": [
        [
          "What makes recursion work?",
          "Each call gets its own frame, so each call's locals are different memory"
        ],
        [
          "Why is n saved before the recursive call?",
          "The call may destroy caller-saved registers, and n is needed after it returns"
        ],
        [
          "How deep can this program recurse?",
          "Stack limit divided by frame size — measured to within 0.07% here"
        ],
        [
          "Does a recursive function recurse at runtime?",
          "Not necessarily; at -O2 both versions became loops"
        ],
        [
          "Why might adding a print break a working program?",
          "It can prevent tail-call elimination and restore the frame growth"
        ]
      ]
    },
    {
      "kind": "try",
      "title": "Extend the measurement",
      "steps": [
        "Add a large local array to the recursive function and re-measure the frame size",
        "Predict the new maximum depth before measuring it",
        "Verify, and explain any discrepancy",
        "Raise the stack limit with ulimit -s and confirm the depth scales as predicted",
        "Write the same algorithm iteratively with an explicit stack and compare the memory used",
        "Measure how deep the iterative version can go before its heap allocation fails",
        "State which of the two you would ship, and why, in terms of the numbers",
        "Find one recursive function in code you already have and compute its safe depth"
      ],
      "expected": "A frame size that grows with the local, a depth that falls in proportion, and a scaling that follows the raised limit. The final step is the one that matters: applying this to code you already own turns the capstone from an exercise into a decision you can defend — and 'we measured the frame at N bytes and the limit at M, so this is safe to depth D' is an argument, where 'recursion is risky' is not."
    }
  ],
  "cheatsheet": [
    "Recursion is ordinary calling; each call gets its own frame, which is why it works",
    "A value needed after the recursive call is spilled to the frame and reloaded",
    "Maximum depth = stack limit divided by frame size",
    "Measured: 32-byte frames, 8176 KB limit, predicted 261,632, measured 261,448 — 0.07% apart",
    "ulimit -s reports the stack limit in kilobytes",
    "A tail call needs no new frame: nothing remains to do after it returns",
    "At -O2 both the tail-recursive and the ordinary factorial became loops with no call",
    "A recursive source function may not recurse at runtime at all",
    "Taking a local's address or adding a print can prevent tail-call elimination",
    "Catch stack overflow with sigaltstack and SA_ONSTACK; use write and hand-formatted digits in the handler — snprintf is not async-signal-safe"
  ],
  "checkpoint": [
    {
      "id": "cp-1",
      "prompt": "In the -O0 factorial, why is n stored to the frame before the recursive call and reloaded after it?",
      "choices": {
        "A": "To satisfy the stack canary guarding the return address",
        "B": "n is needed after the call returns",
        "C": "To make debugging easier",
        "D": "Because n is a parameter"
      },
      "correct": [
        "B"
      ],
      "explanation": "The call may destroy caller-saved registers — rdi in particular will hold a different value inside the callee — so n is spilled to the frame before the call and reloaded after it. That store and load is the entire mechanism of recursion: the outer call's values live in memory the inner call cannot reach, because the inner call's frame is elsewhere."
    },
    {
      "id": "cp-2",
      "prompt": "Frames measure 32 bytes and ulimit -s reports 8176 KB. What maximum recursion depth do you predict?",
      "choices": {
        "A": "About 8,000; measured 8,176",
        "B": "It cannot be predicted",
        "C": "About 32,000",
        "D": "About 261,632"
      },
      "correct": [
        "D"
      ],
      "explanation": "261,448 measured — the shortfall is the frames already on the stack. Two independent measurements and one division, agreeing to within 0.07%. That is what turns 'recursion uses stack' into a quantity you can design with."
    },
    {
      "id": "cp-3",
      "prompt": "A tail-recursive function compiled at -O2 contains no call instruction. What happened?",
      "choices": {
        "A": "The recursion became a loop",
        "B": "The function was inlined into its caller",
        "C": "The compiler removed the function",
        "D": "The recursion moved to the heap"
      },
      "correct": [
        "A"
      ],
      "explanation": "A call in tail position needs no new frame, so the compiler turned it into a backward jump. In the measured example the ordinary non-tail factorial was also turned into a loop, and unrolled eightfold. Recursion in the source says nothing about stack depth in the binary."
    },
    {
      "id": "cp-4",
      "prompt": "A recursive program works, then overflows the stack after someone adds a debug print. Why is that plausible?",
      "choices": {
        "A": "The print destroys tail position",
        "B": "printf is recursive",
        "C": "It is not plausible; the print is unrelated",
        "D": "The print uses too much stack"
      },
      "correct": [
        "A"
      ],
      "explanation": "Tail-call elimination stops, and the per-call frame growth that had been optimised away returns. Taking a local's address or adding work after the recursive call both remove the tail position. This is the first thing to check when a recursive program behaves differently between optimisation levels."
    },
    {
      "id": "cp-5",
      "prompt": "On x86-64, what must a near call preserve so that the callee can return to the instruction after the call?",
      "choices": {
        "A": "The caller's complete register file",
        "B": "The callee's local variables",
        "C": "The return address on the stack",
        "D": "A copy of the function's source code"
      },
      "correct": [
        "C"
      ],
      "explanation": "A call records the address of the next instruction on the stack. ret uses that saved address to resume the caller. Recursive calls work because each invocation gets a distinct return address and its own frame."
    },
    {
      "id": "cp-6",
      "prompt": "After fact(n - 1) returns, the example executes mov rcx, rax and then mov rax, [rbp - 24]. Which values are ready for imul rax, rcx?",
      "choices": {
        "A": "rax holds fact(n - 1); rcx holds n",
        "B": "rax holds n; rcx holds fact(n - 1)",
        "C": "rax and rcx both hold n",
        "D": "rax and rcx both hold the return address"
      },
      "correct": [
        "B"
      ],
      "explanation": "The call returns fact(n - 1) in rax, so the first mov preserves that result in rcx. Reloading [rbp - 24] then restores the outer n into rax. imul combines exactly those two values to produce n × fact(n - 1)."
    },
    {
      "id": "cp-7",
      "prompt": "At the base case of fact(4), which statement best describes the three earlier invocations?",
      "choices": {
        "A": "They share one frame and one return address",
        "B": "They have been moved to heap objects",
        "C": "They no longer exist because the base case ran",
        "D": "Each has its own frame and pending return address"
      },
      "correct": [
        "D"
      ],
      "explanation": "Each call creates its own activation record and leaves a return address for its callee to use. When the base case returns, those calls resume one by one and use their separately saved n values."
    },
    {
      "id": "cp-8",
      "prompt": "In an ordinary function epilogue, what is ret responsible for?",
      "choices": {
        "A": "Allocating the next stack frame",
        "B": "Restoring every caller-saved register",
        "C": "Transferring control to the saved return address",
        "D": "Comparing the recursive argument with the base case"
      },
      "correct": [
        "C"
      ],
      "explanation": "The frame cleanup happens before ret in the example. ret then obtains the saved return address from the stack and transfers control back to the caller, which is the reverse of the call edge."
    },
    {
      "id": "cp-9",
      "prompt": "A measured recursive frame grows from 32 bytes to 96 bytes after adding locals. With the same stack limit, what happens to the predicted maximum depth?",
      "choices": {
        "A": "It triples",
        "B": "It falls to roughly one third",
        "C": "It stays unchanged",
        "D": "It falls by exactly 64 calls"
      },
      "correct": [
        "B"
      ],
      "explanation": "Depth is approximately stack limit divided by frame size. Tripling the measured frame size therefore cuts the predicted depth to one third. Measure again: pre-existing frames and compiler changes can make the observed value differ slightly."
    },
    {
      "id": "cp-10",
      "prompt": "Which source shape leaves a recursive call in tail position?",
      "choices": {
        "A": "return n * fact(n - 1);",
        "B": "long result = fact(n - 1); return result * n;",
        "C": "printf(\"%ld\\n\", fact(n - 1)); return n;",
        "D": "return fact(n - 1);"
      },
      "correct": [
        "D"
      ],
      "explanation": "Only the last form has nothing left to do after the recursive call returns. That makes a tail-call transformation possible when the compiler and ABI conditions permit it; the other forms still need the returned value or perform later work."
    },
    {
      "id": "cp-11",
      "prompt": "A function is tail-recursive in source, but its -O2 disassembly still contains call to itself. What can you conclude from the binary?",
      "choices": {
        "A": "The source is not recursive",
        "B": "The call must overflow the stack immediately",
        "C": "This build did not eliminate that recursive call",
        "D": "The call was converted into a heap allocation"
      },
      "correct": [
        "C"
      ],
      "explanation": "A self-call in the emitted code is direct evidence that this build retained recursion at that site. It does not identify one universal reason: compiler options, surrounding work, ABI constraints, and the exact program can all affect the transformation."
    },
    {
      "id": "cp-12",
      "prompt": "Why does a stack-overflow handler need an alternate signal stack?",
      "choices": {
        "A": "It makes recursive calls use the heap",
        "B": "The normal stack may have no usable space left",
        "C": "It changes the program's stack limit",
        "D": "It preserves caller-saved registers automatically"
      },
      "correct": [
        "B"
      ],
      "explanation": "A SIGSEGV caused by exhausting the stack arrives when the normal stack is unreliable for handler work. sigaltstack provides separate storage, and SA_ONSTACK tells the signal mechanism to run the handler there."
    },
    {
      "id": "cp-13",
      "prompt": "Inside the stack-overflow signal handler, which output approach is appropriate?",
      "choices": {
        "A": "Call printf with a formatted depth",
        "B": "Build a std::string and write it later",
        "C": "Use snprintf, then call malloc",
        "D": "Use write with hand-formatted digits"
      },
      "correct": [
        "D"
      ],
      "explanation": "The lesson's handler uses write and converts digits itself because stdio formatting functions are not async-signal-safe. Keeping the handler to operations that are safe in that context prevents a diagnostic path from failing in a new way."
    },
    {
      "id": "cp-14",
      "prompt": "Which sigaction flag asks the kernel to run a signal handler on the stack registered by sigaltstack?",
      "choices": {
        "A": "SA_RESTART",
        "B": "SA_SIGINFO",
        "C": "SA_ONSTACK",
        "D": "SA_NODEFER"
      },
      "correct": [
        "C"
      ],
      "explanation": "sigaltstack only registers alternate storage. SA_ONSTACK is the handler flag that selects it when the signal is delivered, which is why both pieces appear in the stack-exhaustion example."
    },
    {
      "id": "cp-15",
      "prompt": "The stack-exhaustion experiment reports that sigaction(SIGSEGV, &sa, NULL) returned -1 before recursion begins. What should the program do?",
      "choices": {
        "A": "Continue and assume the handler will still run",
        "B": "Report setup failure and exit before the experiment",
        "C": "Retry only after the stack has overflowed",
        "D": "Replace SIGSEGV with a normal return value"
      },
      "correct": [
        "B"
      ],
      "explanation": "The alternate stack and handler registration are prerequisites for the experiment's controlled failure path. If sigaction fails, continuing would not establish that stack exhaustion is handled safely; the correct result is a visible setup error and an early exit."
    }
  ]
}

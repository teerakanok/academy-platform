const { foundation }                 = require('./foundation')
const { products }                   = require('./products')
const { tailwindPreset, fontFamily } = require('./tailwind')

module.exports = { foundation, products, tailwindPreset, fontFamily }

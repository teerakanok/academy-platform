# dependencies / build
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env — ห้าม secret เข้า git; .env.example เป็น template ที่ต้อง track
.env
.env.*
.dev.vars*
!.env.example

# tests
test-results/
playwright-report/
blob-report/
coverage/

# supabase local runtime
supabase/.branches/
supabase/.temp/

.DS_Store

# ผลลัพธ์ build ของ Cloudflare adapter — สร้างใหม่ได้จาก source เสมอ
.open-next/
.wrangler/

# secret ฝั่ง workerd ตอน dev — ห้ามเข้า git
.dev.vars
